server {
    listen 80;
    server_name _;

    location /uploads/ {
        root /usr/share/nginx/html;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 루트 및 기타 경로 접근 차단
    location / {
        return 403;
    }
}
