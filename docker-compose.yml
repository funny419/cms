services:
  # Database Service (MariaDB)
  db:
    image: mariadb:10.11
    container_name: cms_db
    restart: always
    ports:
      # .gemini.md Rule 3: Host 4807, Internal 3306
      - "4807:3306"
    environment:
      # These variables should be set in a .env file for local development
      # and injected as secrets in the CI/CD pipeline.
      MYSQL_ROOT_PASSWORD: ${CMS_MARIADB_PASSWORD}
      MYSQL_DATABASE: ${CMS_DB_NAME:-cmsdb}
      MYSQL_USER: ${CMS_DB_USER:-funnycms}
      MYSQL_PASSWORD: ${CMS_DB_APP_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Backend Service (Flask)
  backend:
    build: ./backend
    container_name: cms_backend
    restart: always
    ports:
      - "5000:5000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      FLASK_APP: app.py
      FLASK_ENV: development
      FLASK_DEBUG: "1"
      DATABASE_URL: mysql+pymysql://${CMS_DB_USER:-funnycms}:${CMS_DB_APP_PASSWORD}@db:3306/${CMS_DB_NAME:-cmsdb}
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-dev-jwt-secret-key}
      STORAGE_BACKEND: local
    command: ["flask", "run", "--host=0.0.0.0", "--port=5000"]
    volumes:
      - ./backend:/app
      # uploads/는 호스트 ./backend/uploads/ 에 그대로 유지 (dev는 별도 볼륨 불필요)
    develop:
      watch:
        # Python 소스 변경 → 컨테이너에 즉시 동기화 (Flask debug auto-reload 트리거)
        - action: sync
          path: ./backend
          target: /app
          ignore:
            - __pycache__/
            - "**/*.pyc"
            - venv/
            - uploads/
        # requirements.txt 변경 → 이미지 재빌드 (새 패키지 설치)
        - action: rebuild
          path: ./backend/requirements.txt

  # 개발용 파일 서버 — /uploads/ 정적 파일 서빙 (포트 외부 노출 없음)
  nginx-files:
    image: nginx:alpine
    container_name: cms_nginx_files
    restart: always
    volumes:
      - ./backend/uploads:/usr/share/nginx/html/uploads:ro
      - ./nginx/nginx-files.conf:/etc/nginx/conf.d/default.conf:ro

  # Frontend Service (React + Vite)
  frontend:
    build: ./frontend
    container_name: cms_frontend
    restart: always
    ports:
      - "5173:5173"
    environment:
      # Vite proxy가 Docker 내부 네트워크의 backend 서비스로 연결되도록 지정
      BACKEND_URL: http://backend:5000
      FILES_URL: http://nginx-files:80
    # 컨테이너 시작 시마다 npm install 실행 → 이미지 재빌드 없이도 새 패키지 반영
    # npm은 이미 설치된 패키지를 건너뛰므로 변경사항 없을 때 오버헤드 최소
    command: sh -c "npm install && npm run dev -- --host"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    develop:
      watch:
        # src/ 변경 → 즉시 동기화 (Vite HMR 트리거)
        - action: sync
          path: ./frontend/src
          target: /app/src
        # public/ 변경 → 즉시 동기화
        - action: sync
          path: ./frontend/public
          target: /app/public
        # package.json 변경 → 이미지 재빌드 (npm ci 재실행)
        - action: rebuild
          path: ./frontend/package.json

volumes:
  db_data: