# 검색 고도화 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MariaDB Fulltext(ngram) 검색으로 제목+내용 검색 품질을 높이고, 작성자·카테고리·태그 복합 필터를 지원하는 `/search` 전용 페이지를 추가한다.

**Architecture:**
- BE: `list_posts()`의 LIKE → `MATCH...AGAINST BOOLEAN MODE`(ngram 파서) 교체. visibility 조건을 `_vis_cond` 변수로 한 번 계산해 재사용. `?tags=` OR 필터 추가. `/api/auth/users?q=` 작성자 자동완성 API 추가.
- FE: `PostList.jsx`에 `externalFilters` prop 추가해 `Search.jsx`가 재사용. `/search` 초기 상태는 전체 포스트, 키워드 있으면 Fulltext 관련도순 정렬. URL 파라미터 동기화(`useSearchParams`). 제목+excerpt 클라이언트 측 하이라이팅.
- Infra: my.cnf ngram 설정 + docker-compose.yml 마운트. MariaDB 테스트 DB(`cmsdb_test`) 설정.

**Tech Stack:** Python 3.11 + Flask + SQLAlchemy 3.x + MariaDB Fulltext(ngram) / React 19 + Vite + axios + useSearchParams

---

## 파일 변경 맵

### 신규 생성
| 파일 | 역할 |
|------|------|
| `scripts/my.cnf` | MariaDB ngram 설정 (ngram_token_size=2) |
| `scripts/init-test-db.sql` | cmsdb_test DB 초기화 SQL |
| `backend/migrations/versions/xxxx_add_fulltext_index.py` | ngram Fulltext 인덱스 마이그레이션 |
| `backend/tests/test_search.py` | 검색 API 테스트 |
| `frontend/src/pages/Search.jsx` | `/search` 검색 전용 페이지 |

### 수정
| 파일 | 변경 내용 |
|------|---------|
| `docker-compose.yml` | db 서비스에 my.cnf + init-test-db.sql 마운트 |
| `backend/tests/conftest.py` | TestConfig → MariaDB cmsdb_test DB |
| `backend/api/posts.py` | LIKE → Fulltext, `_vis_cond` 변수화, `?tags=` 필터 |
| `backend/api/auth.py` | `GET /api/auth/users?q=` 작성자 자동완성 추가 |
| `frontend/src/api/posts.js` | `listPosts()`에 `tags`, `author`, `sort` 파라미터 추가 |
| `frontend/src/pages/PostList.jsx` | `externalFilters` prop 지원 추가 |
| `frontend/src/App.jsx` | `/search` 라우트 추가 |
| `frontend/src/components/Nav.jsx` | 🔍 검색 아이콘 링크 추가 |

---

## Chunk 1: 인프라 — ngram 설정 + 테스트 DB

### Task 1: my.cnf ngram 설정 + docker-compose 마운트

**Files:**
- Create: `scripts/my.cnf`
- Create: `scripts/init-test-db.sql`
- Modify: `docker-compose.yml`

- [ ] **Step 1: scripts/my.cnf 생성**

```ini
[mysqld]
ngram_token_size=2
innodb_ft_min_token_size=2
ft_min_word_len=2
```

- [ ] **Step 2: scripts/init-test-db.sql 생성**

```sql
CREATE DATABASE IF NOT EXISTS cmsdb_test
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
GRANT ALL PRIVILEGES ON cmsdb_test.* TO 'funnycms'@'%';
FLUSH PRIVILEGES;
```

- [ ] **Step 3: docker-compose.yml db 서비스에 마운트 추가**

`docker-compose.yml`을 읽고 `db` 서비스의 `volumes` 섹션에 추가:
```yaml
volumes:
  - db_data:/var/lib/mysql
  - ./scripts/my.cnf:/etc/mysql/conf.d/custom.cnf:ro
  - ./scripts/init-test-db.sql:/docker-entrypoint-initdb.d/01-test-db.sql:ro
```

> ⚠️ `docker-entrypoint-initdb.d`는 볼륨 최초 초기화 시에만 실행됨. 기존 환경에서는 수동으로 1회 실행 필요.

- [ ] **Step 4: DB 재시작 + ngram 설정 확인**

```bash
docker compose down
docker compose up -d
sleep 10
docker compose exec db mariadb -u funnycms -pfunnycms -e "SHOW VARIABLES LIKE 'ngram_token_size';"
```
예상: `ngram_token_size | 2`

- [ ] **Step 5: cmsdb_test 수동 생성 (기존 환경)**

```bash
docker compose exec db mariadb -u root -e "$(cat scripts/init-test-db.sql)"
```
확인:
```bash
docker compose exec db mariadb -u funnycms -pfunnycms -e "SHOW DATABASES;" | grep cmsdb_test
```
예상: `cmsdb_test` 출력

- [ ] **Step 6: 커밋**

```bash
git add scripts/my.cnf scripts/init-test-db.sql docker-compose.yml
git commit -m "feat: MariaDB ngram Fulltext 설정 + cmsdb_test 테스트 DB"
```

---

### Task 2: conftest.py MariaDB로 전환

**Files:**
- Modify: `backend/tests/conftest.py`

- [ ] **Step 1: conftest.py 읽기 후 TestConfig DB URI 변경**

```python
# backend/tests/conftest.py
class TestConfig:
    TESTING = True
    # ⛔ SQLite 금지 — 반드시 Docker MariaDB 사용
    SQLALCHEMY_DATABASE_URI = "mysql+pymysql://funnycms:funnycms@127.0.0.1:4807/cmsdb_test"
    JWT_SECRET_KEY = "test-secret-key"
    JWT_ACCESS_TOKEN_EXPIRES = False
    SQLALCHEMY_TRACK_MODIFICATIONS = False
    SECRET_KEY = "test-secret"
    STORAGE_BACKEND = "local"
    UPLOAD_FOLDER = "/tmp/cms_test_uploads"
    MAX_CONTENT_LENGTH = 10 * 1024 * 1024
```

> `4807` = docker-compose.yml의 MariaDB 호스트 포트 (`ports: "4807:3306"`)

- [ ] **Step 2: app fixture에서 TESTING 플래그 확인**

`conftest.py`의 `app` fixture에서 `if not app.config.get("TESTING"):`로 마이그레이션 스킵하고 `_db.create_all()`을 호출하는 패턴 그대로 유지. MariaDB에서도 정상 동작.

- [ ] **Step 3: 기존 테스트 통과 확인**

```bash
docker compose exec backend pytest tests/ -v
```
예상: 30개 전체 통과 (MariaDB 연결로)

- [ ] **Step 4: 커밋**

```bash
git add backend/tests/conftest.py
git commit -m "feat: pytest TestConfig → MariaDB cmsdb_test (SQLite 제거)"
```

---

## Chunk 2: BE — Fulltext 마이그레이션 + list_posts() 개선

### Task 3: Fulltext 마이그레이션 (ngram)

**Files:**
- Auto-create: `backend/migrations/versions/xxxx_add_fulltext_index.py`

- [ ] **Step 1: 마이그레이션 파일 생성**

```bash
docker compose exec backend flask db migrate -m "add fulltext ngram index to posts"
```

생성된 파일을 열어 `upgrade()`/`downgrade()` 내용을 아래로 **전체 교체**:

```python
from alembic import op


def upgrade() -> None:
    # ngram 파서로 Fulltext 인덱스 생성 (한글 2자 단위 토크나이징)
    op.execute(
        "ALTER TABLE posts ADD FULLTEXT INDEX ft_posts_search "
        "(title, content, excerpt) WITH PARSER ngram"
    )


def downgrade() -> None:
    op.execute("ALTER TABLE posts DROP INDEX ft_posts_search")
```

- [ ] **Step 2: 마이그레이션 적용**

```bash
docker compose exec backend flask db upgrade
```
또는 백엔드 재시작으로 자동 적용:
```bash
docker compose restart backend
sleep 5
docker compose logs backend --tail=10
```

- [ ] **Step 3: 인덱스 확인**

```bash
docker compose exec db mariadb -u funnycms -pfunnycms cmsdb \
  -e "SHOW INDEX FROM posts WHERE Key_name = 'ft_posts_search';"
```
예상: `ft_posts_search` 행 출력

- [ ] **Step 4: 커밋**

```bash
git add backend/migrations/versions/
git commit -m "feat: posts 테이블 Fulltext ngram 인덱스 추가"
```

---

### Task 4: list_posts() Fulltext 교체 + tags 필터 + 테스트

**Files:**
- Modify: `backend/api/posts.py`
- Create: `backend/tests/test_search.py`

- [ ] **Step 1: 테스트 먼저 작성**

```python
# backend/tests/test_search.py
import uuid
from database import db as _db


def make_post(app, title, content="", status="published", visibility="public"):
    from models.schema import Post, User
    uname = f"u_{uuid.uuid4().hex[:6]}"
    user = User(username=uname, email=f"{uname}@t.com", role="editor")
    user.set_password("p")
    _db.session.add(user)
    _db.session.flush()
    post = Post(
        title=title,
        slug=uuid.uuid4().hex[:8],
        content=content,
        author_id=user.id,
        status=status,
        visibility=visibility,
    )
    _db.session.add(post)
    _db.session.commit()
    return post.id, user.username


def test_search_returns_ok(client, app):
    """?q= 파라미터로 검색 시 200 응답."""
    with app.app_context():
        make_post(app, title="Flask 튜토리얼")

    res = client.get("/api/posts?q=Flask")
    assert res.status_code == 200
    data = res.get_json()["data"]
    assert "items" in data and "total" in data


def test_search_empty_returns_all(client, app):
    """q 없으면 전체 포스트 반환."""
    with app.app_context():
        make_post(app, title="포스트 A")
        make_post(app, title="포스트 B")

    res = client.get("/api/posts")
    assert res.status_code == 200
    assert res.get_json()["data"]["total"] >= 2


def test_tags_filter(client, app):
    """?tags=id 필터로 태그 포스트 조회."""
    with app.app_context():
        from models.schema import Tag
        tag = Tag(name=f"T_{uuid.uuid4().hex[:4]}", slug=f"t-{uuid.uuid4().hex[:4]}")
        _db.session.add(tag)
        _db.session.flush()
        tag_id = tag.id

        post_id, _ = make_post(app, title="태그 포스트")
        from models.schema import Post
        post = _db.session.get(Post, post_id)
        post.tags.append(tag)
        _db.session.commit()

    res = client.get(f"/api/posts?tags={tag_id}")
    assert res.status_code == 200
    assert any(p["id"] == post_id for p in res.get_json()["data"]["items"])


def test_author_filter(client, app):
    """?author=username 필터."""
    with app.app_context():
        post_id, username = make_post(app, title="작성자 필터 테스트")

    res = client.get(f"/api/posts?author={username}")
    assert res.status_code == 200
    assert any(p["id"] == post_id for p in res.get_json()["data"]["items"])
```

- [ ] **Step 2: 테스트 실패 확인 (tags 필터 미구현)**

```bash
docker compose exec backend pytest tests/test_search.py -v
```
예상: `test_tags_filter` FAILED

- [ ] **Step 3: posts.py 수정**

파일을 읽고 아래 3가지 변경 적용:

**3-1. import에 `text` 추가:**
```python
from sqlalchemy import func, or_, select, text
```

**3-2. import에 `PostTag` 추가:**
```python
from models.schema import Comment, Post, PostLike, PostTag, Tag, User
```

**3-3. `list_posts()` — visibility 조건 변수화 + Fulltext 교체 + tags 필터:**

`list_posts()` 함수 내 visibility 필터 블록을 아래로 교체:

```python
# visibility 조건을 변수로 한 번 계산 (모든 쿼리에 재사용)
_user = db.session.get(User, current_user_id) if current_user_id else None
if current_user_id is None:
    _vis_cond = Post.visibility == "public"
elif _user and _user.role != "admin":
    _vis_cond = or_(
        Post.visibility == "public",
        Post.visibility == "members_only",
        (Post.visibility == "private") & (Post.author_id == current_user_id),
    )
else:
    _vis_cond = None  # admin: 전체 조회

if _vis_cond is not None:
    base_query = base_query.where(_vis_cond)
    total_query = total_query.where(_vis_cond)
```

기존 `if q:` 블록을 Fulltext로 교체:
```python
if q:
    ft_filter = text(
        "MATCH(posts.title, posts.content, posts.excerpt) "
        "AGAINST(:q IN BOOLEAN MODE)"
    ).bindparams(q=q)
    base_query = base_query.where(ft_filter)
    total_query = total_query.where(ft_filter)
    # Fulltext 관련도순 정렬 (키워드 있을 때)
    score_col = text(
        "MATCH(posts.title, posts.content, posts.excerpt) "
        "AGAINST(:q2 IN BOOLEAN MODE)"
    ).bindparams(q2=q)
```

기존 `category_id` 필터 블록 아래에 tags 필터 추가:
```python
# tags 필터: ?tags=1,2,3 (OR 방식 — 하나라도 포함)
tags_param = request.args.get("tags", "").strip()
if tags_param:
    tag_ids = [int(t) for t in tags_param.split(",") if t.isdigit()]
    if tag_ids:
        base_query = (
            base_query
            .join(PostTag, PostTag.post_id == Post.id)
            .where(PostTag.tag_id.in_(tag_ids))
            .distinct()
        )
        # total_query 재구성 (tags JOIN + visibility 재적용)
        from sqlalchemy import distinct as sa_distinct
        total_query = select(func.count(sa_distinct(Post.id))).where(
            Post.status == "published"
        ).join(PostTag, PostTag.post_id == Post.id).where(
            PostTag.tag_id.in_(tag_ids)
        )
        if _vis_cond is not None:
            total_query = total_query.where(_vis_cond)
```

정렬 부분 수정 (관련도순 vs 최신순 adaptive):
```python
# 기존:
base_query.order_by(Post.created_at.desc())
# 변경:
if q:
    base_query = base_query.order_by(score_col.desc(), Post.created_at.desc())
else:
    base_query = base_query.order_by(Post.created_at.desc())
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
docker compose exec backend pytest tests/test_search.py -v
docker compose exec backend pytest tests/ -v
```
예상: 전체 통과

- [ ] **Step 5: ruff + mypy**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/posts.py
```

- [ ] **Step 6: 커밋**

```bash
git add backend/api/posts.py backend/tests/test_search.py
git commit -m "feat: list_posts Fulltext ngram + tags OR 필터 + adaptive 정렬"
```

---

### Task 5: 작성자 자동완성 API

**Files:**
- Modify: `backend/api/auth.py`

- [ ] **Step 1: auth.py에 사용자 검색 엔드포인트 추가**

파일 끝에 추가:

```python
@auth_bp.route("/users", methods=["GET"])
def search_users() -> tuple:
    """작성자 자동완성용 유저 검색 (공개)."""
    q = request.args.get("q", "").strip()
    if not q or len(q) < 1:
        return jsonify({"success": True, "data": {"items": []}, "error": ""}), 200

    users = db.session.execute(
        select(User)
        .where(User.username.ilike(f"{q}%"))
        .where(User.role != "deactivated")
        .order_by(User.username)
        .limit(10)
    ).scalars().all()

    items = [{"id": u.id, "username": u.username} for u in users]
    return jsonify({"success": True, "data": {"items": items}, "error": ""}), 200
```

> ⚠️ 기존 `@auth_bp.route("/users")` (admin 전용 list_users)와 충돌 여부 확인. 기존 것이 `@roles_required("admin")`이 붙어 있으므로 GET 동일 경로는 충돌 → 새 엔드포인트는 `/api/auth/users/search`로 분리:

```python
@auth_bp.route("/users/search", methods=["GET"])
def search_users() -> tuple:
```

- [ ] **Step 2: 동작 확인**

```bash
docker compose restart backend
curl "http://localhost:5000/api/auth/users/search?q=ad"
```
예상: `{"success": true, "data": {"items": [{"id": 1, "username": "admin"}]}}`

- [ ] **Step 3: 커밋**

```bash
git add backend/api/auth.py
git commit -m "feat: GET /api/auth/users/search 작성자 자동완성 API"
```

---

## Chunk 3: FE — Search 페이지 + PostList 통합

### Task 6: posts.js 파라미터 확장 + PostList externalFilters + 라우팅

**Files:**
- Modify: `frontend/src/api/posts.js`
- Modify: `frontend/src/pages/PostList.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/components/Nav.jsx`

- [ ] **Step 1: posts.js listPosts 파라미터 확장**

```js
// 기존:
export const listPosts = async (token, page = 1, perPage = 20, q = '', categoryId = null) => {
  const params = { page, per_page: perPage };
  if (q) params.q = q;
  if (categoryId) params.category_id = categoryId;

// 변경:
export const listPosts = async (
  token, page = 1, perPage = 20, q = '', categoryId = null, tagIds = [], author = ''
) => {
  const params = { page, per_page: perPage };
  if (q) params.q = q;
  if (categoryId) params.category_id = categoryId;
  if (tagIds && tagIds.length > 0) params.tags = tagIds.join(',');
  if (author) params.author = author;
```

- [ ] **Step 2: users.js에 자동완성 API 함수 추가**

`frontend/src/api/users.js` 끝에 추가:
```js
export const searchUsers = async (q) => {
  try {
    const response = await axios.get(`/api/auth/users/search`, { params: { q } });
    return response.data;
  } catch (error) {
    return { success: false, error: '사용자 검색에 실패했습니다.' };
  }
};
```

- [ ] **Step 3: PostList.jsx에 externalFilters prop 추가**

`PostList.jsx`를 읽고 컴포넌트 signature에 prop 추가:
```jsx
export default function PostList({ externalFilters = null }) {
```

`fetchFn`에서 externalFilters 우선 적용:
```jsx
const fetchFn = useCallback(
  (page) => {
    const filters = externalFilters || { q, categoryId, tagIds: [], author: '' };
    return listPosts(token, page, 20, filters.q, filters.categoryId, filters.tagIds || [], filters.author || '');
  },
  [token, q, categoryId, externalFilters]
);
const deps = externalFilters
  ? [token, JSON.stringify(externalFilters)]
  : [token, q, categoryId];
const { items: posts, loading, hasMore, error, sentinelRef } = useInfiniteScroll(fetchFn, deps);
```

- [ ] **Step 4: App.jsx에 /search 라우트 추가**

```jsx
import Search from './pages/Search';
// Routes에:
<Route path="/search" element={<Search />} />
```

- [ ] **Step 5: Nav.jsx에 🔍 검색 링크 추가**

비로그인/로그인 공통으로 보이는 영역에 추가 (token 유무 모두):
```jsx
<Link to="/search" className="nav-link" style={{ fontSize: 18 }}>🔍</Link>
```

PostList.jsx 검색창 placeholder 변경:
```jsx
// 기존:
placeholder="제목으로 검색..."
// 변경:
placeholder="포스트 검색..."
```

- [ ] **Step 6: ESLint + 커밋**

```bash
docker compose exec -T frontend npx eslint \
  src/api/posts.js src/api/users.js src/pages/PostList.jsx \
  src/App.jsx src/components/Nav.jsx 2>&1
git add frontend/src/api/posts.js frontend/src/api/users.js \
        frontend/src/pages/PostList.jsx frontend/src/App.jsx \
        frontend/src/components/Nav.jsx
git commit -m "feat: PostList externalFilters prop + Nav 🔍 링크 + API 파라미터 확장"
```

---

### Task 7: Search.jsx 구현

**Files:**
- Create: `frontend/src/pages/Search.jsx`

- [ ] **Step 1: Search.jsx 생성**

```jsx
// frontend/src/pages/Search.jsx
import { useState, useCallback, useEffect } from 'react';
import { useSearchParams } from 'react-router-dom';
import PostList from './PostList';
import CategoryDropdown from '../components/inputs/CategoryDropdown';
import { useCategories } from '../context/CategoryContext';
import { getTags } from '../api/tags';
import { searchUsers } from '../api/users';

// 키워드 하이라이팅: 제목/excerpt에서 일치 텍스트 <mark>로 감싸기
export function highlight(text, q) {
  if (!q || !text) return text;
  const escaped = q.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  const parts = text.split(new RegExp(`(${escaped})`, 'gi'));
  return parts.map((part, i) =>
    part.toLowerCase() === q.toLowerCase()
      ? <mark key={i} style={{ background: 'var(--accent-bg)', color: 'var(--accent-text)', borderRadius: 2 }}>{part}</mark>
      : part
  );
}

export default function Search() {
  const [searchParams, setSearchParams] = useSearchParams();
  const { categories } = useCategories();

  // URL 파라미터로 초기값 설정 (공유/북마크 지원)
  const [inputQ, setInputQ] = useState(searchParams.get('q') || '');
  const [q, setQ] = useState(searchParams.get('q') || '');
  const [categoryId, setCategoryId] = useState(
    searchParams.get('category') ? parseInt(searchParams.get('category')) : null
  );
  const [selectedTagIds, setSelectedTagIds] = useState(
    searchParams.get('tags') ? searchParams.get('tags').split(',').map(Number) : []
  );
  const [inputAuthor, setInputAuthor] = useState(searchParams.get('author') || '');
  const [author, setAuthor] = useState(searchParams.get('author') || '');
  const [allTags, setAllTags] = useState([]);
  const [showAllTags, setShowAllTags] = useState(false);
  const [authorSuggestions, setAuthorSuggestions] = useState([]);

  useEffect(() => {
    getTags().then((res) => { if (res.success) setAllTags(res.data.items || []); });
  }, []);

  // 300ms 디바운스
  useEffect(() => {
    const t = setTimeout(() => setQ(inputQ.trim()), 300);
    return () => clearTimeout(t);
  }, [inputQ]);

  useEffect(() => {
    const t = setTimeout(() => setAuthor(inputAuthor.trim()), 300);
    return () => clearTimeout(t);
  }, [inputAuthor]);

  // 작성자 자동완성
  useEffect(() => {
    if (!inputAuthor.trim()) { setAuthorSuggestions([]); return; }
    const t = setTimeout(() => {
      searchUsers(inputAuthor.trim()).then((res) => {
        if (res.success) setAuthorSuggestions(res.data.items || []);
      });
    }, 300);
    return () => clearTimeout(t);
  }, [inputAuthor]);

  // URL 파라미터 동기화
  useEffect(() => {
    const params = {};
    if (q) params.q = q;
    if (categoryId) params.category = categoryId;
    if (selectedTagIds.length) params.tags = selectedTagIds.join(',');
    if (author) params.author = author;
    setSearchParams(params, { replace: true });
  }, [q, categoryId, selectedTagIds, author]);

  const toggleTag = (tagId) => {
    setSelectedTagIds((prev) =>
      prev.includes(tagId) ? prev.filter((id) => id !== tagId) : [...prev, tagId]
    );
  };

  const hasFilter = q || author || categoryId || selectedTagIds.length > 0;
  const externalFilters = { q, categoryId, tagIds: selectedTagIds, author };

  // 인기 태그 상위 10개
  const visibleTags = showAllTags ? allTags : allTags.slice(0, 10);

  return (
    <div className="page-content" style={{ maxWidth: 900 }}>
      <h1 className="page-heading" style={{ marginBottom: 20 }}>🔍 검색</h1>

      {/* 필터 영역 */}
      <div style={{ background: 'var(--bg-subtle)', borderRadius: 12, padding: 20, marginBottom: 24 }}>

        {/* 키워드 */}
        <div className="form-group" style={{ marginBottom: 14 }}>
          <input
            className="form-input"
            type="text"
            value={inputQ}
            onChange={(e) => setInputQ(e.target.value)}
            placeholder="포스트 검색... (2자 이상)"
            autoFocus
            style={{ fontSize: 16 }}
          />
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12, marginBottom: 14 }}>
          {/* 작성자 */}
          <div style={{ position: 'relative' }}>
            <label className="form-label">작성자</label>
            <input
              className="form-input"
              value={inputAuthor}
              onChange={(e) => setInputAuthor(e.target.value)}
              onBlur={() => setTimeout(() => setAuthorSuggestions([]), 150)}
              placeholder="username 검색..."
            />
            {authorSuggestions.length > 0 && (
              <ul style={{
                position: 'absolute', zIndex: 100, background: 'var(--bg)',
                border: '1px solid var(--border)', borderRadius: 6, padding: 4,
                margin: 0, listStyle: 'none', width: '100%', maxHeight: 160, overflowY: 'auto'
              }}>
                {authorSuggestions.map((u) => (
                  <li key={u.id}
                    onMouseDown={() => { setInputAuthor(u.username); setAuthor(u.username); setAuthorSuggestions([]); }}
                    style={{ padding: '6px 10px', cursor: 'pointer', fontSize: 13 }}>
                    {u.username}
                  </li>
                ))}
              </ul>
            )}
          </div>

          {/* 카테고리 */}
          <div>
            <label className="form-label">카테고리</label>
            <CategoryDropdown
              value={categoryId}
              onChange={setCategoryId}
              categories={categories}
              placeholder="전체 카테고리"
            />
          </div>
        </div>

        {/* 태그 */}
        {allTags.length > 0 && (
          <div>
            <label className="form-label" style={{ marginBottom: 8, display: 'block' }}>태그</label>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6 }}>
              {visibleTags.map((tag) => (
                <span key={tag.id} className="badge"
                  onClick={() => toggleTag(tag.id)}
                  style={{
                    cursor: 'pointer', userSelect: 'none',
                    opacity: selectedTagIds.includes(tag.id) ? 1 : 0.45,
                    outline: selectedTagIds.includes(tag.id) ? '2px solid var(--accent)' : 'none',
                  }}>
                  #{tag.name}
                </span>
              ))}
              {allTags.length > 10 && (
                <button className="btn btn-ghost" style={{ fontSize: 12, padding: '2px 8px' }}
                  onClick={() => setShowAllTags((p) => !p)}>
                  {showAllTags ? '접기' : `+${allTags.length - 10}개 더`}
                </button>
              )}
            </div>
          </div>
        )}

        {/* 필터 초기화 */}
        {hasFilter && (
          <button className="btn btn-ghost" style={{ marginTop: 12, fontSize: 13 }}
            onClick={() => {
              setInputQ(''); setQ('');
              setInputAuthor(''); setAuthor('');
              setCategoryId(null); setSelectedTagIds([]);
            }}>
            필터 초기화
          </button>
        )}
      </div>

      {/* 결과 — PostList 재사용 (externalFilters prop) */}
      <PostList externalFilters={externalFilters} highlightQ={q} />
    </div>
  );
}
```

- [ ] **Step 2: PostList.jsx에 하이라이팅 지원 추가**

`PostList.jsx`를 읽고 수정:

import 추가:
```jsx
import { highlight } from './Search';
```

포스트 제목/excerpt 렌더링 부분 수정:
```jsx
// 기존:
<div className="post-title">{post.title}</div>
{post.excerpt && <div className="post-excerpt">{post.excerpt}</div>}

// 변경:
<div className="post-title">
  {highlightQ ? highlight(post.title, highlightQ) : post.title}
</div>
{post.excerpt && (
  <div className="post-excerpt">
    {highlightQ ? highlight(post.excerpt, highlightQ) : post.excerpt}
  </div>
)}
```

`PostList` 컴포넌트 signature에 `highlightQ` prop 추가:
```jsx
export default function PostList({ externalFilters = null, highlightQ = '' }) {
```

> ⚠️ 순환 import 방지: `highlight` 함수를 `frontend/src/utils/highlight.js`로 분리하는 것이 더 안전함.

- [ ] **Step 3: highlight 함수 분리 (순환 import 방지)**

```js
// frontend/src/utils/highlight.jsx
export function highlight(text, q) {
  if (!q || !text) return text;
  const escaped = q.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  const parts = text.split(new RegExp(`(${escaped})`, 'gi'));
  return parts.map((part, i) =>
    part.toLowerCase() === q.toLowerCase()
      ? <mark key={i} style={{ background: 'var(--accent-bg)', color: 'var(--accent-text)', borderRadius: 2 }}>{part}</mark>
      : part
  );
}
```

Search.jsx와 PostList.jsx 모두에서 `import { highlight } from '../utils/highlight';`로 변경.

- [ ] **Step 4: ESLint + 확인**

```bash
docker compose exec -T frontend npx eslint \
  src/pages/Search.jsx src/pages/PostList.jsx src/utils/highlight.jsx 2>&1
```

브라우저 확인:
1. `/search` 접속 → 전체 포스트 목록 표시
2. 키워드 입력 → 제목/excerpt 하이라이팅 확인
3. 태그 chip 클릭 → 필터 적용 + URL 변경 확인
4. 작성자 입력 → 자동완성 드롭다운 확인
5. URL 복사 후 새 탭 → 동일 필터 상태 복원 확인

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/pages/Search.jsx frontend/src/pages/PostList.jsx \
        frontend/src/utils/highlight.jsx
git commit -m "feat: /search 페이지 (복합 필터 + URL 동기화 + 하이라이팅)"
```

---

## 완료 체크리스트

- [ ] `docker compose exec backend pytest tests/ -v` — 전체 통과
- [ ] `SHOW INDEX FROM posts WHERE Key_name = 'ft_posts_search'` — ngram 인덱스 확인
- [ ] `/search?q=Flask` → 관련도순 결과 + 하이라이팅
- [ ] `/search?tags=1` → 태그 필터 결과
- [ ] `/search?author=admin` → 작성자 필터 결과
- [ ] URL 복사 후 새 탭 → 동일 필터 복원
- [ ] GitHub Projects `[Phase 1] 검색 고도화` → Done 변경
