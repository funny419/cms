# Sprint 2 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 유저별 블로그(`/blog/:username`) 기초 → 태그 시스템 → 카테고리 시스템 → 블로그 홈 완성 순으로 구현해 멀티블로그 플랫폼 정체성을 확립한다.

**Architecture:** 기획팀 권고에 따라 사용자 가치 체감 순서로 구현. User/Post 기존 구조를 활용해 `blogs` 테이블 없이 유저별 블로그를 구현(Sprint 3에서 분리 예정). Tag는 명시적 ORM 모델(PostTag) + Replace 방식 업데이트. Category는 3단 깊이 제한 + API 레벨 검증.

**Tech Stack:** Python 3.11 + Flask + SQLAlchemy 3.x + pytest + `python-slugify` / React 19 + Vite + axios + Context API

---

## 파일 변경 맵

### 신규 생성 (BE)
| 파일 | 역할 |
|------|------|
| `backend/api/tags.py` | 태그 CRUD 5개 엔드포인트 |
| `backend/api/categories.py` | 카테고리 CRUD 6개 엔드포인트 |
| `backend/tests/test_users.py` | 유저 블로그 프로필 API 테스트 |
| `backend/tests/test_tags.py` | 태그 CRUD + 포스트-태그 연결 테스트 |
| `backend/tests/test_categories.py` | 카테고리 CRUD 테스트 |
| `backend/migrations/versions/xxxx_create_tags_post_tags.py` | 마이그레이션 |
| `backend/migrations/versions/xxxx_create_categories_add_category_id.py` | 마이그레이션 |

### 신규 생성 (FE)
| 파일 | 역할 |
|------|------|
| `frontend/src/api/tags.js` | 태그 API 함수 |
| `frontend/src/api/categories.js` | 카테고리 API 함수 |
| `frontend/src/api/users.js` | 유저 프로필 API 함수 |
| `frontend/src/components/inputs/TagInput.jsx` | chip 형태 태그 입력 |
| `frontend/src/components/inputs/CategoryDropdown.jsx` | 카테고리 단일 선택 셀렉트 |
| `frontend/src/components/widgets/TagCloud.jsx` | 태그 클라우드 위젯 |
| `frontend/src/components/widgets/CategorySidebar.jsx` | 카테고리 트리 사이드바 |
| `frontend/src/components/ProfileCard.jsx` | 유저 프로필 카드 |
| `frontend/src/pages/BlogHome.jsx` | `/blog/:username` 페이지 |
| `frontend/src/context/CategoryContext.jsx` | 전역 카테고리 목록 Context |

### 수정 (BE)
| 파일 | 변경 내용 |
|------|---------|
| `backend/requirements.txt` | `python-slugify` 추가 |
| `backend/models/schema.py` | Tag, PostTag, Category 추가 + Post에 category_id/tags |
| `backend/api/auth.py` | `GET /api/users/:username` 추가 |
| `backend/api/posts.py` | create/update/list에 category_id, tags 처리 |
| `backend/app.py` | tags_bp, categories_bp Blueprint 등록 |

### 수정 (FE)
| 파일 | 변경 내용 |
|------|---------|
| `frontend/src/App.jsx` | `/blog/:username` 라우트 추가, CategoryProvider 래핑 |
| `frontend/src/components/Nav.jsx` | editor role에 "내 블로그" 링크 추가 |
| `frontend/src/pages/PostEditor.jsx` | category_id + tags 상태/UI 추가 |
| `frontend/src/pages/PostDetail.jsx` | TagCloud 추가 |
| `frontend/src/pages/PostList.jsx` | CategorySidebar + category 필터 |
| `frontend/src/pages/admin/AdminPosts.jsx` | CategoryDropdown 필터 추가 |
| `frontend/src/api/posts.js` | category, tags 파라미터 추가 |

---

## Chunk 1: 유저별 블로그 기초

### Task 1: `GET /api/users/:username` BE API

**Files:**
- Modify: `backend/api/auth.py`
- Create: `backend/tests/test_users.py`

- [ ] **Step 1: 테스트 먼저 작성**

```python
# backend/tests/test_users.py
def test_get_user_profile(client, app):
    with app.app_context():
        from models.schema import User
        from database import db as _db
        user = User(username="alice", email="alice@test.com", role="editor",
                    bio="개발자입니다", avatar_url="/uploads/alice.jpg")
        user.set_password("pass")
        _db.session.add(user)
        _db.session.commit()

    res = client.get("/api/users/alice")
    assert res.status_code == 200
    data = res.get_json()["data"]
    assert data["username"] == "alice"
    assert data["bio"] == "개발자입니다"
    assert "post_count" in data


def test_get_nonexistent_user_returns_404(client):
    res = client.get("/api/users/nobody")
    assert res.status_code == 404


def test_deactivated_user_returns_404(client, app):
    with app.app_context():
        from models.schema import User
        from database import db as _db
        user = User(username="banned", email="banned@test.com", role="deactivated")
        user.set_password("pass")
        _db.session.add(user)
        _db.session.commit()

    res = client.get("/api/users/banned")
    assert res.status_code == 404
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
docker compose exec backend pytest tests/test_users.py -v
```
예상: FAILED (엔드포인트 없음)

- [ ] **Step 3: auth.py에 엔드포인트 추가**

`backend/api/auth.py` 파일 끝에 추가:

```python
@auth_bp.route("/users/<username>", methods=["GET"])
def get_user_profile(username: str) -> tuple:
    """유저 블로그 프로필 조회 — 비로그인도 접근 가능."""
    user: User | None = db.session.execute(
        select(User).where(User.username == username)
    ).scalar_one_or_none()

    if not user or user.role == "deactivated":
        return jsonify({"success": False, "data": {}, "error": "User not found"}), 404

    post_count: int = db.session.execute(
        select(func.count(Post.id)).where(
            (Post.author_id == user.id)
            & (Post.status == "published")
            & (Post.visibility.in_(["public", "members_only"]))
        )
    ).scalar() or 0

    d = user.to_dict()
    d["post_count"] = post_count
    return jsonify({"success": True, "data": d, "error": ""}), 200
```

> ⚠️ `Post`를 import 해야 함 — auth.py 상단에 `from models.schema import Post, User` 확인/추가

- [ ] **Step 4: 테스트 통과 확인**

```bash
docker compose exec backend pytest tests/test_users.py -v
```
예상: 3개 PASSED

- [ ] **Step 5: 전체 회귀 확인**

```bash
docker compose exec backend pytest tests/ -v
```
예상: 전체 통과

- [ ] **Step 6: ruff + mypy**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/auth.py
```

- [ ] **Step 7: 커밋**

```bash
git add backend/api/auth.py backend/tests/test_users.py
git commit -m "feat: GET /api/users/:username 유저 블로그 프로필 API"
```

---

### Task 2: BlogHome FE 페이지 + 라우팅

**Files:**
- Create: `frontend/src/api/users.js`
- Create: `frontend/src/pages/BlogHome.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/components/Nav.jsx`

- [ ] **Step 1: users.js API 함수 생성**

```js
// frontend/src/api/users.js
import axios from 'axios';

const authHeader = (token) => ({ Authorization: `Bearer ${token}` });

export const getUserProfile = async (username) => {
  try {
    const response = await axios.get(`/api/auth/users/${username}`);
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '프로필을 불러오지 못했습니다.' };
  }
};

export const getUserPosts = async (username, token, page = 1, perPage = 20) => {
  try {
    const headers = token ? authHeader(token) : {};
    const response = await axios.get('/api/posts', {
      headers,
      params: { page, per_page: perPage, author: username },
    });
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '포스트를 불러오지 못했습니다.' };
  }
};
```

> ⚠️ BE `GET /api/posts?author=:username` 필터는 Task 10에서 추가됨. 이 Task에서는 BlogHome이 `/api/posts?author=` 파라미터를 사용하지 않고, 전체 포스트 목록에서 FE 필터링 대신 author_username 필드를 활용. BE posts.py에 author 필터 추가는 Chunk 3에서 진행.

- [ ] **Step 2: BlogHome.jsx 생성 (기초 버전)**

```jsx
// frontend/src/pages/BlogHome.jsx
import { useState, useEffect, useCallback } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import { getUserProfile } from '../api/users';
import { listPosts } from '../api/posts';
import useInfiniteScroll from '../hooks/useInfiniteScroll';

export default function BlogHome() {
  const { username } = useParams();
  const navigate = useNavigate();
  const token = localStorage.getItem('token');

  const [profile, setProfile] = useState(null);
  const [profileLoading, setProfileLoading] = useState(true);
  const [profileError, setProfileError] = useState('');

  // 프로필 로드
  useEffect(() => {
    setProfileLoading(true);
    getUserProfile(username).then((res) => {
      if (res.success) {
        setProfile(res.data);
      } else {
        setProfileError(res.error || '사용자를 찾을 수 없습니다.');
      }
      setProfileLoading(false);
    });
  }, [username]);

  // 포스트 목록 (해당 유저의 published 포스트만)
  // TODO Chunk 4: author 필터 BE 구현 후 ?author=username 파라미터 추가
  const fetchFn = useCallback(
    (page) => listPosts(token, page, 20, ''),
    [token]
  );
  const { items: posts, loading, hasMore, sentinelRef } = useInfiniteScroll(
    fetchFn,
    [token]
  );

  // 해당 username의 포스트만 필터 (임시 — BE author 필터 추가 전)
  const userPosts = posts.filter((p) => p.author_username === username);

  if (profileLoading) return (
    <div className="empty-state" style={{ marginTop: 80 }}>불러오는 중...</div>
  );

  if (profileError) return (
    <div className="page-content">
      <div className="alert alert-error">{profileError}</div>
    </div>
  );

  return (
    <div className="page-content" style={{ maxWidth: 800 }}>
      {/* 프로필 헤더 */}
      <div style={{ display: 'flex', gap: 16, alignItems: 'flex-start', marginBottom: 32,
        padding: 24, background: 'var(--bg-subtle)', borderRadius: 12 }}>
        {profile.avatar_url && (
          <img
            src={profile.avatar_url}
            alt={profile.username}
            style={{ width: 64, height: 64, borderRadius: '50%', objectFit: 'cover',
              border: '2px solid var(--border)', flexShrink: 0 }}
            onError={(e) => { e.target.style.display = 'none'; }}
          />
        )}
        <div>
          <h1 style={{ fontSize: 22, fontWeight: 700, marginBottom: 4 }}>
            {profile.username}의 블로그
          </h1>
          {profile.bio && (
            <p style={{ color: 'var(--text-light)', fontSize: 14, marginBottom: 8 }}>
              {profile.bio}
            </p>
          )}
          <span style={{ fontSize: 12, color: 'var(--text-light)' }}>
            포스트 {profile.post_count}개
          </span>
        </div>
      </div>

      {/* 포스트 목록 */}
      <h2 style={{ fontSize: 16, fontWeight: 600, marginBottom: 16 }}>최근 글</h2>

      {userPosts.length === 0 && !loading ? (
        <div className="empty-state"><p>게시된 글이 없습니다.</p></div>
      ) : (
        <ul className="post-list">
          {userPosts.map((post) => (
            <li key={post.id} className="post-item"
              onClick={() => navigate(`/posts/${post.id}`)}
              style={{ cursor: 'pointer' }}>
              <div className="post-title">{post.title}</div>
              {post.excerpt && <div className="post-excerpt">{post.excerpt}</div>}
              <div className="post-meta">
                <span>{new Date(post.created_at).toLocaleDateString('ko-KR')}</span>
                <span>·</span>
                <span>👁 {post.view_count ?? 0}</span>
              </div>
            </li>
          ))}
        </ul>
      )}

      <div ref={sentinelRef} style={{ height: 1 }} />
      {loading && <div className="empty-state" style={{ marginTop: 24 }}>불러오는 중...</div>}
    </div>
  );
}
```

- [ ] **Step 3: App.jsx에 라우트 추가**

`/posts/:id` Route **위에** 추가:
```jsx
<Route path="/blog/:username" element={<BlogHome />} />
```

import 추가:
```jsx
import BlogHome from './pages/BlogHome';
```

- [ ] **Step 4: Nav.jsx에 "내 블로그" 링크 추가**

editor 링크 목록에 추가:
```jsx
<Link to={`/blog/${user?.username}`} className="nav-link">내 블로그</Link>
```

기존 `<Link to="/my-posts" ...>내 글</Link>` 아래에 삽입.

- [ ] **Step 5: ESLint 확인**

```bash
docker compose exec -T frontend npx eslint src/pages/BlogHome.jsx src/api/users.js src/App.jsx src/components/Nav.jsx 2>&1
```
예상: 에러 없음

- [ ] **Step 6: 동작 확인**

브라우저에서 `/blog/{존재하는_username}` 접속 → 프로필 헤더 + 포스트 목록 표시 확인.

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/api/users.js frontend/src/pages/BlogHome.jsx \
        frontend/src/App.jsx frontend/src/components/Nav.jsx
git commit -m "feat: /blog/:username 유저별 블로그 페이지 기초"
```

---

## Chunk 2: 태그 시스템

### Task 3: requirements.txt + Tag/PostTag 스키마 + 마이그레이션

**Files:**
- Modify: `backend/requirements.txt`
- Modify: `backend/models/schema.py`
- Auto-create: `backend/migrations/versions/xxxx_create_tags_post_tags.py`

- [ ] **Step 1: python-slugify 추가**

`backend/requirements.txt` 끝에 추가:
```
python-slugify
```

Docker 이미지 재빌드:
```bash
docker compose up -d --build backend
```

확인:
```bash
docker compose exec backend python -c "from slugify import slugify; print(slugify('Python 기초'))"
```
예상: `python-gi` 또는 `python` 형태 출력

- [ ] **Step 2: schema.py에 Tag, PostTag 추가**

`backend/models/schema.py`의 `UniqueConstraint` import 이미 있음 확인. 파일 끝 (MenuItem 클래스 아래)에 추가:

```python
class Tag(Base):
    """태그 (계층 없음, 단순 라벨)"""

    __tablename__ = "tags"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(50), nullable=False, unique=True)
    slug: Mapped[str] = mapped_column(String(50), nullable=False, unique=True, index=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())

    # Relationships
    posts: Mapped[List["Post"]] = relationship(
        "Post", secondary="post_tags", back_populates="tags"
    )

    def to_dict(self, post_count: int = 0) -> dict:
        return {
            "id": self.id,
            "name": self.name,
            "slug": self.slug,
            "post_count": post_count,
            "created_at": self.created_at.isoformat() if self.created_at else None,
        }


class PostTag(Base):
    """포스트-태그 다대다 연결 테이블"""

    __tablename__ = "post_tags"

    id: Mapped[int] = mapped_column(primary_key=True)
    post_id: Mapped[int] = mapped_column(
        ForeignKey("posts.id", ondelete="CASCADE"), nullable=False
    )
    tag_id: Mapped[int] = mapped_column(
        ForeignKey("tags.id", ondelete="CASCADE"), nullable=False
    )
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())

    __table_args__ = (UniqueConstraint("post_id", "tag_id", name="uq_post_tag"),)
```

`Post` 클래스에 추가 (relationship + to_dict):

```python
# Post 클래스의 Relationships 섹션에 추가:
tags: Mapped[List["Tag"]] = relationship(
    "Tag", secondary="post_tags", back_populates="posts"
)

# Post.to_dict()에 추가:
"tags": [{"id": t.id, "name": t.name, "slug": t.slug} for t in self.tags],
```

- [ ] **Step 3: 마이그레이션 생성**

```bash
docker compose exec backend flask db migrate -m "create tags and post_tags"
```

생성된 파일 확인 — 아래 내용이 있어야 함:
- `CREATE TABLE tags`
- `CREATE TABLE post_tags`
- `UNIQUE constraint (post_id, tag_id)`

- [ ] **Step 4: 백엔드 재시작**

```bash
docker compose restart backend && sleep 5
docker compose logs backend --tail=10
```
예상: 마이그레이션 성공 로그

- [ ] **Step 5: mypy + 커밋**

```bash
docker compose exec backend mypy models/schema.py
```

```bash
git add backend/requirements.txt backend/models/schema.py \
        backend/migrations/versions/
git commit -m "feat: Tag/PostTag 스키마 추가 + python-slugify"
```

---

### Task 4: 태그 API (tags.py) + 테스트

**Files:**
- Create: `backend/api/tags.py`
- Create: `backend/tests/test_tags.py`
- Modify: `backend/app.py`

- [ ] **Step 1: 테스트 먼저 작성**

```python
# backend/tests/test_tags.py
from database import db as _db


def make_tag(app, name="Python", slug="python"):
    from models.schema import Tag
    tag = Tag(name=name, slug=slug)
    _db.session.add(tag)
    _db.session.commit()
    return tag.id


def test_list_tags(client):
    res = client.get("/api/tags")
    assert res.status_code == 200
    data = res.get_json()["data"]
    assert "items" in data


def test_create_tag_requires_admin(client, editor_headers):
    res = client.post("/api/tags", json={"name": "Python"}, headers=editor_headers)
    assert res.status_code == 403


def test_create_tag(client, app, admin_headers):
    res = client.post("/api/tags", json={"name": "Python"}, headers=admin_headers)
    assert res.status_code == 201
    data = res.get_json()["data"]
    assert data["name"] == "Python"
    assert data["slug"] == "python"  # python-slugify 자동 생성


def test_delete_tag(client, app, admin_headers):
    with app.app_context():
        tag_id = make_tag(app)
    res = client.delete(f"/api/tags/{tag_id}", headers=admin_headers)
    assert res.status_code == 200


def test_delete_nonexistent_tag(client, admin_headers):
    res = client.delete("/api/tags/99999", headers=admin_headers)
    assert res.status_code == 404
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
docker compose exec backend pytest tests/test_tags.py -v
```
예상: FAILED

- [ ] **Step 3: tags.py 생성**

```python
# backend/api/tags.py
from flask import Blueprint, jsonify, request
from slugify import slugify
from sqlalchemy import func, select
from sqlalchemy.exc import IntegrityError

from api.decorators import roles_required
from database import db
from models.schema import Post, PostTag, Tag

tags_bp = Blueprint("tags", __name__, url_prefix="/api/tags")


@tags_bp.route("", methods=["GET"])
def list_tags() -> tuple:
    """태그 목록 (포스트 수 포함)."""
    page = max(1, request.args.get("page", 1, type=int) or 1)
    per_page = min(max(1, request.args.get("per_page", 50, type=int) or 50), 100)
    offset = (page - 1) * per_page

    count_sq = (
        select(func.count(PostTag.id))
        .where(PostTag.tag_id == Tag.id)
        .correlate(Tag)
        .scalar_subquery()
    )
    total: int = db.session.execute(select(func.count(Tag.id))).scalar() or 0
    tags = db.session.execute(
        select(Tag, count_sq.label("post_count"))
        .order_by(count_sq.desc())
        .offset(offset)
        .limit(per_page)
    ).all()

    items = [t.to_dict(post_count=pc or 0) for t, pc in tags]
    return jsonify(
        {"success": True, "data": {"items": items, "total": total}, "error": ""}
    ), 200


@tags_bp.route("", methods=["POST"])
@roles_required("admin")
def create_tag() -> tuple:
    """태그 생성 (admin only)."""
    data: dict = request.get_json() or {}
    name = data.get("name", "").strip()
    if not name:
        return jsonify({"success": False, "data": {}, "error": "name is required"}), 400

    slug = data.get("slug") or slugify(name, allow_unicode=False)
    tag = Tag(name=name, slug=slug)
    db.session.add(tag)
    try:
        db.session.commit()
    except IntegrityError:
        db.session.rollback()
        return jsonify({"success": False, "data": {}, "error": "Tag already exists"}), 400
    return jsonify({"success": True, "data": tag.to_dict(), "error": ""}), 201


@tags_bp.route("/<int:tag_id>", methods=["GET"])
def get_tag(tag_id: int) -> tuple:
    """태그 단건 조회."""
    tag: Tag | None = db.session.get(Tag, tag_id)
    if not tag:
        return jsonify({"success": False, "data": {}, "error": "Tag not found"}), 404
    post_count: int = (
        db.session.execute(
            select(func.count(PostTag.id)).where(PostTag.tag_id == tag_id)
        ).scalar()
        or 0
    )
    return jsonify({"success": True, "data": tag.to_dict(post_count=post_count), "error": ""}), 200


@tags_bp.route("/<int:tag_id>", methods=["DELETE"])
@roles_required("admin")
def delete_tag(tag_id: int) -> tuple:
    """태그 삭제 (PostTag cascade)."""
    tag: Tag | None = db.session.get(Tag, tag_id)
    if not tag:
        return jsonify({"success": False, "data": {}, "error": "Tag not found"}), 404
    db.session.delete(tag)
    try:
        db.session.commit()
    except Exception:
        db.session.rollback()
        return jsonify({"success": False, "data": {}, "error": "An internal error occurred."}), 500
    return jsonify({"success": True, "data": {}, "error": ""}), 200


@tags_bp.route("/<int:tag_id>/posts", methods=["GET"])
def list_tag_posts(tag_id: int) -> tuple:
    """태그별 포스트 목록."""
    from flask_jwt_extended import get_jwt_identity, verify_jwt_in_request
    from sqlalchemy import or_

    from models.schema import User

    tag: Tag | None = db.session.get(Tag, tag_id)
    if not tag:
        return jsonify({"success": False, "data": {}, "error": "Tag not found"}), 404

    try:
        verify_jwt_in_request(optional=True)
        raw_id = get_jwt_identity()
        current_user_id = int(raw_id) if raw_id else None
    except Exception:
        current_user_id = None

    page = max(1, request.args.get("page", 1, type=int) or 1)
    per_page = min(max(1, request.args.get("per_page", 20, type=int) or 20), 100)
    offset = (page - 1) * per_page

    from models.schema import Post

    base = (
        select(Post)
        .join(PostTag, PostTag.post_id == Post.id)
        .where(PostTag.tag_id == tag_id)
        .where(Post.status == "published")
    )
    if current_user_id is None:
        base = base.where(Post.visibility == "public")
    else:
        _user = db.session.get(User, current_user_id)
        if not (_user and _user.role == "admin"):
            base = base.where(
                or_(
                    Post.visibility == "public",
                    Post.visibility == "members_only",
                    (Post.visibility == "private") & (Post.author_id == current_user_id),
                )
            )

    total: int = db.session.execute(
        select(func.count()).select_from(base.subquery())
    ).scalar() or 0
    posts = db.session.execute(
        base.order_by(Post.created_at.desc()).offset(offset).limit(per_page)
    ).scalars().all()

    return jsonify(
        {
            "success": True,
            "data": {
                "items": [p.to_dict() for p in posts],
                "page": page,
                "per_page": per_page,
                "total": total,
                "has_more": page * per_page < total,
            },
            "error": "",
        }
    ), 200
```

- [ ] **Step 4: app.py에 Blueprint 등록**

`app.py`의 Blueprint 등록 섹션에 추가:
```python
from api.tags import tags_bp
app.register_blueprint(tags_bp)
```

- [ ] **Step 5: 테스트 통과 + 전체 회귀**

```bash
docker compose exec backend pytest tests/test_tags.py -v
docker compose exec backend pytest tests/ -v
```
예상: 전체 통과

- [ ] **Step 6: ruff + mypy + 커밋**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/tags.py
git add backend/api/tags.py backend/tests/test_tags.py backend/app.py
git commit -m "feat: 태그 CRUD API (GET/POST/DELETE + 태그별 포스트 목록)"
```

---

### Task 5: posts.py 태그 처리 + FE 태그 UI

**Files:**
- Modify: `backend/api/posts.py`
- Create: `frontend/src/api/tags.js`
- Create: `frontend/src/components/inputs/TagInput.jsx`
- Create: `frontend/src/components/widgets/TagCloud.jsx`
- Modify: `frontend/src/pages/PostEditor.jsx`
- Modify: `frontend/src/pages/PostDetail.jsx`

- [ ] **Step 1: posts.py에 tags 처리 추가**

`backend/api/posts.py` 상단 import 수정:
```python
from models.schema import Comment, Post, PostLike, PostTag, Tag, User
```

`create_post()` 함수에서 `db.session.add(post)` **아래**, `try:` **위에** 추가:
```python
db.session.flush()  # post.id 생성
tag_ids: list = data.get("tags", [])
if tag_ids:
    tags = db.session.execute(
        select(Tag).where(Tag.id.in_(tag_ids))
    ).scalars().all()
    post.tags.extend(tags)
```

`update_post()` 함수에서 `for field in (...)` 블록 **아래**에 추가:
```python
if "tags" in data:
    post.tags.clear()
    if data["tags"]:
        new_tags = db.session.execute(
            select(Tag).where(Tag.id.in_(data["tags"]))
        ).scalars().all()
        post.tags.extend(new_tags)
```

- [ ] **Step 2: 기존 태그 테스트로 posts.py 검증**

```python
# backend/tests/test_tags.py에 추가:
def test_create_post_with_tags(client, app, editor_headers):
    with app.app_context():
        tag1_id = make_tag(app, "Python", "python")
        tag2_id = make_tag(app, "Django", "django")

    res = client.post(
        "/api/posts",
        json={"title": "Django 튜토리얼", "tags": [tag1_id, tag2_id], "status": "published"},
        headers=editor_headers,
    )
    assert res.status_code == 201
    assert len(res.get_json()["data"]["tags"]) == 2


def test_update_post_tags_replaces(client, app, editor_headers):
    with app.app_context():
        t1 = make_tag(app, "Tag1", "tag1")
        t2 = make_tag(app, "Tag2", "tag2")

    # 포스트 생성 with [t1, t2]
    res = client.post("/api/posts",
        json={"title": "Test", "tags": [t1, t2]}, headers=editor_headers)
    post_id = res.get_json()["data"]["id"]

    # 수정 with [t2]만
    res = client.put(f"/api/posts/{post_id}",
        json={"tags": [t2]}, headers=editor_headers)
    assert res.status_code == 200
    assert len(res.get_json()["data"]["tags"]) == 1
    assert res.get_json()["data"]["tags"][0]["id"] == t2
```

```bash
docker compose exec backend pytest tests/test_tags.py -v
```

- [ ] **Step 3: FE tags.js 생성**

```js
// frontend/src/api/tags.js
import axios from 'axios';

const authHeader = (token) => ({ Authorization: `Bearer ${token}` });

export const getTags = async () => {
  try {
    const response = await axios.get('/api/tags', { params: { per_page: 100 } });
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '태그를 불러오지 못했습니다.' };
  }
};

export const createTag = async (token, name) => {
  try {
    const response = await axios.post('/api/tags', { name }, { headers: authHeader(token) });
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '태그 생성에 실패했습니다.' };
  }
};
```

- [ ] **Step 4: TagInput.jsx 생성**

```jsx
// frontend/src/components/inputs/TagInput.jsx
import { useState } from 'react';

/**
 * TagInput — chip 형태 태그 입력
 * Props:
 *   selectedTags: [{ id, name, slug }]  — 현재 선택된 태그
 *   availableTags: [{ id, name, slug }] — 전체 태그 목록 (자동완성용)
 *   onChange: (tags) => void            — 변경 시 콜백
 */
export default function TagInput({ selectedTags = [], availableTags = [], onChange }) {
  const [input, setInput] = useState('');
  const [showSuggestions, setShowSuggestions] = useState(false);

  const suggestions = availableTags.filter(
    (t) =>
      input &&
      t.name.toLowerCase().includes(input.toLowerCase()) &&
      !selectedTags.find((s) => s.id === t.id)
  );

  const addTag = (tag) => {
    if (!selectedTags.find((t) => t.id === tag.id)) {
      onChange([...selectedTags, tag]);
    }
    setInput('');
    setShowSuggestions(false);
  };

  const removeTag = (tagId) => {
    onChange(selectedTags.filter((t) => t.id !== tagId));
  };

  return (
    <div style={{ position: 'relative' }}>
      {/* 선택된 태그 chips */}
      <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, marginBottom: 6 }}>
        {selectedTags.map((tag) => (
          <span key={tag.id} className="badge"
            style={{ display: 'flex', alignItems: 'center', gap: 4 }}>
            #{tag.name}
            <button
              type="button"
              onClick={() => removeTag(tag.id)}
              style={{ background: 'none', border: 'none', cursor: 'pointer',
                color: 'inherit', fontSize: 12, padding: 0, lineHeight: 1 }}>
              ×
            </button>
          </span>
        ))}
      </div>

      {/* 입력 */}
      <input
        className="form-input"
        value={input}
        onChange={(e) => { setInput(e.target.value); setShowSuggestions(true); }}
        onFocus={() => setShowSuggestions(true)}
        onBlur={() => setTimeout(() => setShowSuggestions(false), 150)}
        placeholder="태그 검색..."
        style={{ fontSize: 13 }}
      />

      {/* 자동완성 드롭다운 */}
      {showSuggestions && suggestions.length > 0 && (
        <ul style={{ position: 'absolute', zIndex: 100, background: 'var(--bg)',
          border: '1px solid var(--border)', borderRadius: 6, padding: 4,
          margin: 0, listStyle: 'none', width: '100%', maxHeight: 160, overflowY: 'auto' }}>
          {suggestions.map((tag) => (
            <li key={tag.id}
              onMouseDown={() => addTag(tag)}
              style={{ padding: '6px 10px', cursor: 'pointer', borderRadius: 4,
                fontSize: 13 }}
              onMouseEnter={(e) => { e.target.style.background = 'var(--bg-subtle)'; }}
              onMouseLeave={(e) => { e.target.style.background = 'transparent'; }}>
              #{tag.name}
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 5: TagCloud.jsx 생성**

```jsx
// frontend/src/components/widgets/TagCloud.jsx
import { useNavigate } from 'react-router-dom';

/**
 * TagCloud — 태그 목록을 chip 형태로 표시
 * Props:
 *   tags: [{ id, name, slug, post_count? }]
 */
export default function TagCloud({ tags = [] }) {
  const navigate = useNavigate();
  if (!tags.length) return null;

  return (
    <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6 }}>
      {tags.map((tag) => (
        <span
          key={tag.id}
          className="badge"
          onClick={() => navigate(`/posts?tag=${tag.id}`)}
          style={{ cursor: 'pointer', userSelect: 'none' }}
        >
          #{tag.name}
          {tag.post_count > 0 && (
            <span style={{ marginLeft: 4, opacity: 0.7 }}>({tag.post_count})</span>
          )}
        </span>
      ))}
    </div>
  );
}
```

- [ ] **Step 6: PostEditor.jsx에 태그 상태/UI 추가**

`form` useState에 `tags: []` 추가.

편집 로드 시 `setForm` 블록에 추가:
```js
tags: res.data.tags || [],
```

`useState` 섹션에 추가:
```js
const [availableTags, setAvailableTags] = useState([]);
```

`useEffect` (기존 로드 아래)에 추가:
```js
useEffect(() => {
  getTags().then((res) => { if (res.success) setAvailableTags(res.data.items); });
}, []);
```

import 추가:
```js
import { getTags } from '../api/tags';
import TagInput from '../components/inputs/TagInput';
```

하단 옵션 grid 아래에 태그 섹션 추가:
```jsx
<div style={{ marginTop: 16 }}>
  <label className="form-label">태그</label>
  <TagInput
    selectedTags={form.tags}
    availableTags={availableTags}
    onChange={(tags) => setForm({ ...form, tags })}
  />
</div>
```

`handleSave`의 payload에서 tags를 ID 배열로 변환:
```js
const payload = {
  ...form,
  tags: form.tags.map((t) => t.id),
};
```

- [ ] **Step 7: PostDetail.jsx에 TagCloud 추가**

import 추가:
```js
import TagCloud from '../components/widgets/TagCloud';
```

본문 하단 (추천 버튼 영역 아래)에 추가:
```jsx
{post.tags && post.tags.length > 0 && (
  <div style={{ marginTop: 24, paddingTop: 16, borderTop: '1px solid var(--border)' }}>
    <p style={{ fontSize: 13, color: 'var(--text-light)', marginBottom: 8 }}>태그</p>
    <TagCloud tags={post.tags} />
  </div>
)}
```

- [ ] **Step 8: ESLint + 커밋**

```bash
docker compose exec -T frontend npx eslint src/components/inputs/TagInput.jsx \
  src/components/widgets/TagCloud.jsx src/api/tags.js \
  src/pages/PostEditor.jsx src/pages/PostDetail.jsx 2>&1
```

```bash
git add backend/api/posts.py backend/tests/test_tags.py \
        frontend/src/api/tags.js \
        frontend/src/components/inputs/TagInput.jsx \
        frontend/src/components/widgets/TagCloud.jsx \
        frontend/src/pages/PostEditor.jsx \
        frontend/src/pages/PostDetail.jsx
git commit -m "feat: 태그 시스템 FE (TagInput + TagCloud) + posts.py 태그 처리"
```

---

## Chunk 3: 카테고리 시스템

### Task 6: Category 스키마 + Post.category_id + 마이그레이션

**Files:**
- Modify: `backend/models/schema.py`
- Auto-create: `backend/migrations/versions/xxxx_create_categories.py`

- [ ] **Step 1: schema.py에 Category 추가**

파일 끝 (PostTag 아래)에 추가:

```python
MAX_CATEGORY_DEPTH = 3  # 카테고리 최대 깊이 (API 레벨 검증)


class Category(Base):
    """카테고리 (계층형, 최대 3단)"""

    __tablename__ = "categories"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    slug: Mapped[str] = mapped_column(String(100), nullable=False, unique=True, index=True)
    description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    parent_id: Mapped[Optional[int]] = mapped_column(
        ForeignKey("categories.id", ondelete="SET NULL"), nullable=True
    )
    order: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())

    # Relationships (자기참조)
    posts: Mapped[List["Post"]] = relationship("Post", back_populates="category")
    parent: Mapped[Optional["Category"]] = relationship(
        "Category", back_populates="children",
        remote_side="Category.id", foreign_keys="[Category.parent_id]"
    )
    children: Mapped[List["Category"]] = relationship(
        "Category", back_populates="parent", foreign_keys="[Category.parent_id]"
    )

    def to_dict(self, post_count: int = 0) -> dict:
        return {
            "id": self.id,
            "name": self.name,
            "slug": self.slug,
            "description": self.description,
            "parent_id": self.parent_id,
            "order": self.order,
            "post_count": post_count,
            "created_at": self.created_at.isoformat() if self.created_at else None,
        }
```

`Post` 클래스에 `category_id` 추가:

```python
# Post 클래스의 visibility 컬럼 아래에:
category_id: Mapped[Optional[int]] = mapped_column(
    ForeignKey("categories.id", ondelete="SET NULL"), nullable=True
)

# Relationships에:
category: Mapped[Optional["Category"]] = relationship("Category", back_populates="posts")

# Post.to_dict()에:
"category_id": self.category_id,
```

- [ ] **Step 2: 마이그레이션 생성**

```bash
docker compose exec backend flask db migrate -m "create categories add category_id to posts"
```

생성된 파일 확인:
- `CREATE TABLE categories`
- `ALTER TABLE posts ADD COLUMN category_id INT`
- `category_id` nullable=True (server_default 불필요)

- [ ] **Step 3: 백엔드 재시작**

```bash
docker compose restart backend && sleep 5
docker compose logs backend --tail=10
```

- [ ] **Step 4: mypy + 커밋**

```bash
docker compose exec backend mypy models/schema.py
git add backend/models/schema.py backend/migrations/versions/
git commit -m "feat: Category 스키마 추가 + Post.category_id FK"
```

---

### Task 7: 카테고리 API + 테스트

**Files:**
- Create: `backend/api/categories.py`
- Create: `backend/tests/test_categories.py`
- Modify: `backend/app.py`

- [ ] **Step 1: 테스트 먼저 작성**

```python
# backend/tests/test_categories.py
from database import db as _db


def make_category(app, name="기술", slug="tech", parent_id=None):
    from models.schema import Category
    cat = Category(name=name, slug=slug, parent_id=parent_id)
    _db.session.add(cat)
    _db.session.commit()
    return cat.id


def test_list_categories(client):
    res = client.get("/api/categories")
    assert res.status_code == 200
    assert "items" in res.get_json()["data"]


def test_create_category_requires_admin(client, editor_headers):
    res = client.post("/api/categories", json={"name": "기술"}, headers=editor_headers)
    assert res.status_code == 403


def test_create_category(client, app, admin_headers):
    res = client.post("/api/categories", json={"name": "기술"}, headers=admin_headers)
    assert res.status_code == 201
    data = res.get_json()["data"]
    assert data["name"] == "기술"
    assert data["slug"] == "gisul"  # python-slugify 한글 처리


def test_create_child_category(client, app, admin_headers):
    with app.app_context():
        parent_id = make_category(app)
    res = client.post(
        "/api/categories",
        json={"name": "Python", "parent_id": parent_id},
        headers=admin_headers,
    )
    assert res.status_code == 201
    assert res.get_json()["data"]["parent_id"] == parent_id


def test_depth_limit_enforced(client, app, admin_headers):
    """깊이 3단 초과 시 400."""
    with app.app_context():
        d1 = make_category(app, "D1", "d1")
        d2 = make_category(app, "D2", "d2", parent_id=d1)
        d3 = make_category(app, "D3", "d3", parent_id=d2)
    res = client.post(
        "/api/categories",
        json={"name": "D4", "parent_id": d3},
        headers=admin_headers,
    )
    assert res.status_code == 400


def test_delete_category(client, app, admin_headers):
    with app.app_context():
        cat_id = make_category(app, "삭제대상", "delete-me")
    res = client.delete(f"/api/categories/{cat_id}", headers=admin_headers)
    assert res.status_code == 200
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
docker compose exec backend pytest tests/test_categories.py -v
```
예상: FAILED

- [ ] **Step 3: categories.py 생성**

```python
# backend/api/categories.py
from flask import Blueprint, jsonify, request
from slugify import slugify
from sqlalchemy import func, select
from sqlalchemy.exc import IntegrityError

from api.decorators import roles_required
from database import db
from models.schema import MAX_CATEGORY_DEPTH, Category, Post

categories_bp = Blueprint("categories", __name__, url_prefix="/api/categories")


def _get_depth(category_id: int | None) -> int:
    """카테고리 깊이 계산 (루트=1)."""
    if not category_id:
        return 0
    depth = 0
    current_id: int | None = category_id
    while current_id is not None:
        cat: Category | None = db.session.get(Category, current_id)
        if not cat:
            break
        depth += 1
        current_id = cat.parent_id
        if depth > MAX_CATEGORY_DEPTH + 1:  # 무한루프 방지
            break
    return depth


@categories_bp.route("", methods=["GET"])
def list_categories() -> tuple:
    """카테고리 목록 (포스트 수 포함, flat list)."""
    count_sq = (
        select(func.count(Post.id))
        .where(Post.category_id == Category.id)
        .where(Post.status == "published")
        .correlate(Category)
        .scalar_subquery()
    )
    cats = db.session.execute(
        select(Category, count_sq.label("post_count"))
        .order_by(Category.parent_id.asc().nulls_first(), Category.order.asc())
    ).all()

    items = [c.to_dict(post_count=pc or 0) for c, pc in cats]
    return jsonify(
        {"success": True, "data": {"items": items, "total": len(items)}, "error": ""}
    ), 200


@categories_bp.route("", methods=["POST"])
@roles_required("admin")
def create_category() -> tuple:
    """카테고리 생성 (admin only, 깊이 3단 제한)."""
    data: dict = request.get_json() or {}
    name = data.get("name", "").strip()
    if not name:
        return jsonify({"success": False, "data": {}, "error": "name is required"}), 400

    parent_id: int | None = data.get("parent_id")

    # 깊이 검사
    if parent_id:
        parent_depth = _get_depth(parent_id)
        if parent_depth >= MAX_CATEGORY_DEPTH:
            return jsonify(
                {"success": False, "data": {}, "error": f"카테고리 깊이는 {MAX_CATEGORY_DEPTH}단까지만 허용됩니다."}
            ), 400

    # 같은 부모 내 이름 중복 검사
    existing = db.session.execute(
        select(Category).where(
            (Category.name == name) & (Category.parent_id == parent_id)
        )
    ).scalar_one_or_none()
    if existing:
        return jsonify({"success": False, "data": {}, "error": "같은 부모 카테고리 내에 같은 이름이 존재합니다."}), 400

    slug = data.get("slug") or slugify(name, allow_unicode=False)
    cat = Category(
        name=name,
        slug=slug,
        description=data.get("description"),
        parent_id=parent_id,
        order=data.get("order", 0),
    )
    db.session.add(cat)
    try:
        db.session.commit()
    except IntegrityError:
        db.session.rollback()
        return jsonify({"success": False, "data": {}, "error": "Slug already exists"}), 400
    return jsonify({"success": True, "data": cat.to_dict(), "error": ""}), 201


@categories_bp.route("/<int:cat_id>", methods=["GET"])
def get_category(cat_id: int) -> tuple:
    """카테고리 단건 + 자식 목록."""
    cat: Category | None = db.session.get(Category, cat_id)
    if not cat:
        return jsonify({"success": False, "data": {}, "error": "Category not found"}), 404
    post_count: int = (
        db.session.execute(
            select(func.count(Post.id))
            .where(Post.category_id == cat_id)
            .where(Post.status == "published")
        ).scalar()
        or 0
    )
    d = cat.to_dict(post_count=post_count)
    d["children"] = [c.to_dict() for c in cat.children]
    return jsonify({"success": True, "data": d, "error": ""}), 200


@categories_bp.route("/<int:cat_id>", methods=["PUT"])
@roles_required("admin")
def update_category(cat_id: int) -> tuple:
    """카테고리 수정."""
    cat: Category | None = db.session.get(Category, cat_id)
    if not cat:
        return jsonify({"success": False, "data": {}, "error": "Category not found"}), 404
    data: dict = request.get_json() or {}
    for field in ("name", "slug", "description", "parent_id", "order"):
        if field in data:
            setattr(cat, field, data[field])
    try:
        db.session.commit()
    except Exception:
        db.session.rollback()
        return jsonify({"success": False, "data": {}, "error": "An internal error occurred."}), 500
    return jsonify({"success": True, "data": cat.to_dict(), "error": ""}), 200


@categories_bp.route("/<int:cat_id>", methods=["DELETE"])
@roles_required("admin")
def delete_category(cat_id: int) -> tuple:
    """카테고리 삭제 (Post.category_id → NULL 처리)."""
    cat: Category | None = db.session.get(Category, cat_id)
    if not cat:
        return jsonify({"success": False, "data": {}, "error": "Category not found"}), 404
    db.session.delete(cat)
    try:
        db.session.commit()
    except Exception:
        db.session.rollback()
        return jsonify({"success": False, "data": {}, "error": "An internal error occurred."}), 500
    return jsonify({"success": True, "data": {}, "error": ""}), 200


@categories_bp.route("/<int:cat_id>/posts", methods=["GET"])
def list_category_posts(cat_id: int) -> tuple:
    """카테고리별 포스트 목록."""
    from flask_jwt_extended import get_jwt_identity, verify_jwt_in_request
    from sqlalchemy import or_
    from models.schema import User

    cat: Category | None = db.session.get(Category, cat_id)
    if not cat:
        return jsonify({"success": False, "data": {}, "error": "Category not found"}), 404

    try:
        verify_jwt_in_request(optional=True)
        raw_id = get_jwt_identity()
        current_user_id = int(raw_id) if raw_id else None
    except Exception:
        current_user_id = None

    page = max(1, request.args.get("page", 1, type=int) or 1)
    per_page = min(max(1, request.args.get("per_page", 20, type=int) or 20), 100)
    offset = (page - 1) * per_page

    base = select(Post).where(Post.category_id == cat_id).where(Post.status == "published")
    if current_user_id is None:
        base = base.where(Post.visibility == "public")
    else:
        _user = db.session.get(User, current_user_id)
        if not (_user and _user.role == "admin"):
            base = base.where(
                or_(
                    Post.visibility == "public",
                    Post.visibility == "members_only",
                    (Post.visibility == "private") & (Post.author_id == current_user_id),
                )
            )

    total: int = db.session.execute(
        select(func.count()).select_from(base.subquery())
    ).scalar() or 0
    posts = db.session.execute(
        base.order_by(Post.created_at.desc()).offset(offset).limit(per_page)
    ).scalars().all()

    return jsonify(
        {
            "success": True,
            "data": {
                "items": [p.to_dict() for p in posts],
                "page": page, "per_page": per_page,
                "total": total, "has_more": page * per_page < total,
            },
            "error": "",
        }
    ), 200
```

- [ ] **Step 4: app.py에 categories_bp 등록**

```python
from api.categories import categories_bp
app.register_blueprint(categories_bp)
```

- [ ] **Step 5: 테스트 통과 + 전체 회귀**

```bash
docker compose exec backend pytest tests/test_categories.py -v
docker compose exec backend pytest tests/ -v
```

- [ ] **Step 6: ruff + mypy + 커밋**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/categories.py
git add backend/api/categories.py backend/tests/test_categories.py backend/app.py
git commit -m "feat: 카테고리 CRUD API (깊이 3단 제한 + 포스트별 목록)"
```

---

### Task 8: posts.py 카테고리 처리 + FE 카테고리 UI

**Files:**
- Modify: `backend/api/posts.py`
- Create: `frontend/src/api/categories.js`
- Create: `frontend/src/context/CategoryContext.jsx`
- Create: `frontend/src/components/inputs/CategoryDropdown.jsx`
- Create: `frontend/src/components/widgets/CategorySidebar.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/pages/PostEditor.jsx`
- Modify: `frontend/src/pages/PostList.jsx`
- Modify: `frontend/src/pages/admin/AdminPosts.jsx`

- [ ] **Step 1: posts.py 카테고리 처리 추가**

`backend/api/posts.py`의 import에 `Category` 추가:
```python
from models.schema import Category, Comment, Post, PostLike, PostTag, Tag, User
```

`create_post()` — Post 생성 시 `category_id` 추가:
```python
post = Post(
    ...
    visibility=data.get("visibility", "public"),
    category_id=data.get("category_id"),  # ← 추가
)
```

`update_post()` — field 목록에 `category_id` 추가:
```python
for field in ("title", "slug", "content", "excerpt", "status", "post_type",
              "content_format", "visibility", "category_id"):  # ← category_id 추가
```

`list_posts()` — category_id 필터 추가 (visibility 필터 블록 아래):
```python
category_id = request.args.get("category_id", type=int)
if category_id:
    base_query = base_query.where(Post.category_id == category_id)
    total_query = total_query.where(Post.category_id == category_id)
```

- [ ] **Step 2: FE categories.js 생성**

```js
// frontend/src/api/categories.js
import axios from 'axios';

export const getCategories = async () => {
  try {
    const response = await axios.get('/api/categories');
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '카테고리를 불러오지 못했습니다.' };
  }
};
```

- [ ] **Step 3: CategoryContext.jsx 생성**

```jsx
// frontend/src/context/CategoryContext.jsx
import { createContext, useContext, useEffect, useState } from 'react';
import { getCategories } from '../api/categories';

const CategoryContext = createContext({ categories: [], loading: true });

export function CategoryProvider({ children }) {
  const [categories, setCategories] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    getCategories().then((res) => {
      if (res.success) setCategories(res.data.items);
      setLoading(false);
    });
  }, []);

  return (
    <CategoryContext.Provider value={{ categories, loading }}>
      {children}
    </CategoryContext.Provider>
  );
}

export const useCategories = () => useContext(CategoryContext);
```

- [ ] **Step 4: App.jsx에 CategoryProvider 추가**

```jsx
import { CategoryProvider } from './context/CategoryContext';
import BlogHome from './pages/BlogHome';
```

`SkinProvider` **안쪽**에 래핑:
```jsx
<SkinProvider>
  <CategoryProvider>
    <AppContent />
  </CategoryProvider>
</SkinProvider>
```

- [ ] **Step 5: CategoryDropdown.jsx 생성**

```jsx
// frontend/src/components/inputs/CategoryDropdown.jsx
/**
 * CategoryDropdown — 카테고리 단일 선택 셀렉트
 * Props:
 *   value: number | null
 *   onChange: (categoryId: number | null) => void
 *   categories: [{ id, name, parent_id }]
 *   placeholder: string
 */
export default function CategoryDropdown({
  value = null,
  onChange,
  categories = [],
  placeholder = '카테고리 선택 (선택사항)',
}) {
  return (
    <select
      className="form-input"
      value={value ?? ''}
      onChange={(e) => onChange(e.target.value ? parseInt(e.target.value) : null)}
    >
      <option value="">{placeholder}</option>
      {categories.map((cat) => (
        <option key={cat.id} value={cat.id}>
          {cat.parent_id ? '  └ ' : ''}{cat.name}
        </option>
      ))}
    </select>
  );
}
```

- [ ] **Step 6: CategorySidebar.jsx 생성**

```jsx
// frontend/src/components/widgets/CategorySidebar.jsx
/**
 * CategorySidebar — 카테고리 필터 사이드바
 * Props:
 *   categories: [{ id, name, parent_id, post_count }]
 *   selectedId: number | null
 *   onSelect: (id: number | null) => void
 */
export default function CategorySidebar({ categories = [], selectedId, onSelect }) {
  if (!categories.length) return null;

  const roots = categories.filter((c) => !c.parent_id);
  const children = (parentId) => categories.filter((c) => c.parent_id === parentId);

  return (
    <div style={{ marginBottom: 24 }}>
      <p style={{ fontSize: 12, fontWeight: 600, color: 'var(--text-light)',
        textTransform: 'uppercase', letterSpacing: 1, marginBottom: 8 }}>
        📁 카테고리
      </p>
      <ul style={{ listStyle: 'none', padding: 0, margin: 0 }}>
        <li>
          <button
            onClick={() => onSelect(null)}
            style={{
              background: 'none', border: 'none', cursor: 'pointer', padding: '4px 0',
              fontSize: 14, color: selectedId == null ? 'var(--accent)' : 'var(--text)',
              fontWeight: selectedId == null ? 600 : 400,
            }}>
            전체 ({categories.reduce((s, c) => s + (c.post_count || 0), 0)})
          </button>
        </li>
        {roots.map((cat) => (
          <li key={cat.id}>
            <button
              onClick={() => onSelect(cat.id)}
              style={{
                background: 'none', border: 'none', cursor: 'pointer', padding: '4px 0',
                fontSize: 14, color: selectedId === cat.id ? 'var(--accent)' : 'var(--text)',
                fontWeight: selectedId === cat.id ? 600 : 400,
              }}>
              {cat.name} ({cat.post_count || 0})
            </button>
            {children(cat.id).map((child) => (
              <button key={child.id}
                onClick={() => onSelect(child.id)}
                style={{
                  display: 'block', background: 'none', border: 'none', cursor: 'pointer',
                  padding: '3px 0 3px 16px', fontSize: 13,
                  color: selectedId === child.id ? 'var(--accent)' : 'var(--text-light)',
                }}>
                └ {child.name} ({child.post_count || 0})
              </button>
            ))}
          </li>
        ))}
      </ul>
    </div>
  );
}
```

- [ ] **Step 7: PostEditor.jsx에 카테고리 추가**

`useCategories` import 추가:
```js
import { useCategories } from '../context/CategoryContext';
import CategoryDropdown from '../components/inputs/CategoryDropdown';
```

컴포넌트 내에서:
```js
const { categories } = useCategories();
```

`form` 상태에 `category_id: null` 추가.

편집 로드 시 `setForm` 블록에:
```js
category_id: res.data.category_id ?? null,
```

하단 옵션 grid의 타입 `<select>` 아래에 추가:
```jsx
<div>
  <label className="form-label">카테고리</label>
  <CategoryDropdown
    value={form.category_id}
    onChange={(id) => setForm({ ...form, category_id: id })}
    categories={categories}
  />
</div>
```

- [ ] **Step 8: PostList.jsx에 카테고리 필터 추가**

```js
import { useCategories } from '../context/CategoryContext';
import CategorySidebar from '../components/widgets/CategorySidebar';
```

```js
const { categories } = useCategories();
const [categoryId, setCategoryId] = useState(null);
```

`fetchFn` 수정:
```js
const fetchFn = useCallback(
  (page) => listPosts(token, page, 20, q, categoryId),
  [token, q, categoryId]
);
```

레이아웃을 2열(사이드바 + 목록)로:
```jsx
<div style={{ display: 'flex', gap: 24 }}>
  <aside style={{ width: 160, flexShrink: 0 }}>
    <CategorySidebar
      categories={categories}
      selectedId={categoryId}
      onSelect={(id) => { setCategoryId(id); }}
    />
  </aside>
  <div style={{ flex: 1 }}>
    {/* 기존 검색 + 목록 코드 */}
  </div>
</div>
```

- [ ] **Step 9: AdminPosts.jsx에 카테고리 필터 추가**

기존 status 필터 셀렉트 옆에 CategoryDropdown 추가:
```jsx
<CategoryDropdown
  value={category}
  onChange={setCategory}
  categories={categories}
  placeholder="전체 카테고리"
/>
```

- [ ] **Step 10: posts.js에 파라미터 추가**

```js
// 기존 listPosts 시그니처에 categoryId 추가:
export const listPosts = async (token, page = 1, perPage = 20, q = '', categoryId = null) => {
  const params = { page, per_page: perPage };
  if (q) params.q = q;
  if (categoryId) params.category_id = categoryId;
  // ...
};
```

- [ ] **Step 11: ESLint + 전체 테스트**

```bash
docker compose exec backend pytest tests/ -v
docker compose exec -T frontend npx eslint src/api/categories.js \
  src/context/CategoryContext.jsx src/components/inputs/CategoryDropdown.jsx \
  src/components/widgets/CategorySidebar.jsx src/pages/PostEditor.jsx \
  src/pages/PostList.jsx 2>&1
```

- [ ] **Step 12: 커밋**

```bash
git add backend/api/posts.py \
        frontend/src/api/categories.js frontend/src/api/posts.js \
        frontend/src/context/CategoryContext.jsx \
        frontend/src/components/inputs/CategoryDropdown.jsx \
        frontend/src/components/widgets/CategorySidebar.jsx \
        frontend/src/App.jsx frontend/src/pages/PostEditor.jsx \
        frontend/src/pages/PostList.jsx frontend/src/pages/admin/AdminPosts.jsx
git commit -m "feat: 카테고리 시스템 FE (CategoryDropdown + CategorySidebar + PostList 필터)"
```

---

## Chunk 4: 블로그 홈 완성

### Task 9: BlogHome 완성 + 기존 페이지 통합

**Files:**
- Create: `frontend/src/components/ProfileCard.jsx`
- Modify: `frontend/src/pages/BlogHome.jsx`
- Modify: `frontend/src/api/users.js` (author 필터 추가)
- Modify: `backend/api/posts.py` (author 필터 추가)

- [ ] **Step 1: posts.py에 author 필터 추가**

`list_posts()` — category_id 필터 아래에 추가:
```python
author_username = request.args.get("author", "").strip()
if author_username:
    author_user = db.session.execute(
        select(User).where(User.username == author_username)
    ).scalar_one_or_none()
    if author_user:
        base_query = base_query.where(Post.author_id == author_user.id)
        total_query = total_query.where(Post.author_id == author_user.id)
    else:
        base_query = base_query.where(Post.author_id == -1)  # 없는 사용자 = 빈 결과
        total_query = total_query.where(Post.author_id == -1)
```

- [ ] **Step 2: users.js에 getUserPosts 수정**

```js
// frontend/src/api/users.js (수정)
export const getUserPosts = async (username, token, page = 1, perPage = 20) => {
  try {
    const headers = token ? authHeader(token) : {};
    const response = await axios.get('/api/posts', {
      headers,
      params: { page, per_page: perPage, author: username },  // ← author 파라미터 사용
    });
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '포스트를 불러오지 못했습니다.' };
  }
};
```

- [ ] **Step 3: ProfileCard.jsx 생성**

```jsx
// frontend/src/components/ProfileCard.jsx
export default function ProfileCard({ user }) {
  if (!user) return null;
  return (
    <div style={{ display: 'flex', gap: 16, alignItems: 'flex-start', marginBottom: 32,
      padding: 24, background: 'var(--bg-subtle)', borderRadius: 12 }}>
      {user.avatar_url && (
        <img
          src={user.avatar_url}
          alt={user.username}
          style={{ width: 64, height: 64, borderRadius: '50%', objectFit: 'cover',
            border: '2px solid var(--border)', flexShrink: 0 }}
          onError={(e) => { e.target.style.display = 'none'; }}
        />
      )}
      <div>
        <h1 style={{ fontSize: 22, fontWeight: 700, marginBottom: 4 }}>
          {user.username}의 블로그
        </h1>
        {user.bio && (
          <p style={{ color: 'var(--text-light)', fontSize: 14, marginBottom: 8 }}>
            {user.bio}
          </p>
        )}
        <span style={{ fontSize: 12, color: 'var(--text-light)' }}>
          포스트 {user.post_count}개 · {new Date(user.created_at).getFullYear()}년 시작
        </span>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: BlogHome.jsx 완성 버전으로 교체**

기존 BlogHome.jsx를 아래로 교체:

```jsx
// frontend/src/pages/BlogHome.jsx
import { useState, useEffect, useCallback } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import { getUserProfile, getUserPosts } from '../api/users';
import useInfiniteScroll from '../hooks/useInfiniteScroll';
import ProfileCard from '../components/ProfileCard';
import CategorySidebar from '../components/widgets/CategorySidebar';
import TagCloud from '../components/widgets/TagCloud';
import { getCategories } from '../api/categories';

export default function BlogHome() {
  const { username } = useParams();
  const navigate = useNavigate();
  const token = localStorage.getItem('token');

  const [profile, setProfile] = useState(null);
  const [profileLoading, setProfileLoading] = useState(true);
  const [profileError, setProfileError] = useState('');
  const [categories, setCategories] = useState([]);
  const [categoryId, setCategoryId] = useState(null);

  useEffect(() => {
    setProfileLoading(true);
    Promise.all([
      getUserProfile(username),
      getCategories(),
    ]).then(([profileRes, catRes]) => {
      if (profileRes.success) setProfile(profileRes.data);
      else setProfileError(profileRes.error || '사용자를 찾을 수 없습니다.');
      if (catRes.success) setCategories(catRes.data.items);
      setProfileLoading(false);
    });
  }, [username]);

  const fetchFn = useCallback(
    (page) => getUserPosts(username, token, page, 20),
    [username, token]
  );
  const { items: posts, loading, hasMore, sentinelRef } = useInfiniteScroll(
    fetchFn, [username, token]
  );

  // 카테고리 필터 (FE 필터링 — BE author + category 복합 필터는 향후 최적화)
  const filteredPosts = categoryId
    ? posts.filter((p) => p.category_id === categoryId)
    : posts;

  if (profileLoading) return (
    <div className="empty-state" style={{ marginTop: 80 }}>불러오는 중...</div>
  );
  if (profileError) return (
    <div className="page-content"><div className="alert alert-error">{profileError}</div></div>
  );

  return (
    <div className="page-content" style={{ maxWidth: 900 }}>
      <ProfileCard user={profile} />

      <div style={{ display: 'flex', gap: 32 }}>
        {/* 사이드바 */}
        <aside style={{ width: 160, flexShrink: 0 }}>
          <CategorySidebar
            categories={categories}
            selectedId={categoryId}
            onSelect={setCategoryId}
          />
        </aside>

        {/* 포스트 목록 */}
        <div style={{ flex: 1 }}>
          <h2 style={{ fontSize: 16, fontWeight: 600, marginBottom: 16 }}>
            {categoryId
              ? `${categories.find((c) => c.id === categoryId)?.name || '카테고리'} 글`
              : '최근 글'}
          </h2>

          {filteredPosts.length === 0 && !loading ? (
            <div className="empty-state"><p>게시된 글이 없습니다.</p></div>
          ) : (
            <ul className="post-list">
              {filteredPosts.map((post) => (
                <li key={post.id} className="post-item"
                  onClick={() => navigate(`/posts/${post.id}`)}
                  style={{ cursor: 'pointer' }}>
                  <div className="post-title">{post.title}</div>
                  {post.excerpt && <div className="post-excerpt">{post.excerpt}</div>}
                  {post.tags && post.tags.length > 0 && (
                    <TagCloud tags={post.tags} />
                  )}
                  <div className="post-meta">
                    <span>{new Date(post.created_at).toLocaleDateString('ko-KR')}</span>
                    <span>·</span><span>👁 {post.view_count ?? 0}</span>
                    <span>·</span><span>♥ {post.like_count ?? 0}</span>
                  </div>
                </li>
              ))}
            </ul>
          )}

          <div ref={sentinelRef} style={{ height: 1 }} />
          {loading && <div className="empty-state" style={{ marginTop: 24 }}>불러오는 중...</div>}
          {!hasMore && filteredPosts.length > 0 && (
            <div style={{ textAlign: 'center', color: 'var(--text-light)', fontSize: 13, padding: '24px 0' }}>
              더 이상 글이 없습니다.
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: 전체 테스트 + ESLint**

```bash
docker compose exec backend pytest tests/ -v
docker compose exec -T frontend npx eslint \
  src/components/ProfileCard.jsx src/pages/BlogHome.jsx src/api/users.js 2>&1
```

- [ ] **Step 6: 최종 커밋**

```bash
git add backend/api/posts.py \
        frontend/src/api/users.js \
        frontend/src/components/ProfileCard.jsx \
        frontend/src/pages/BlogHome.jsx
git commit -m "feat: 블로그 홈 완성 (ProfileCard + CategorySidebar + TagCloud 통합)"
```

---

## Sprint 2 완료 체크리스트

- [ ] `docker compose exec backend pytest tests/ -v` — 전체 통과 (14+ 테스트)
- [ ] `docker compose exec backend ruff check .` — 에러 없음
- [ ] `/blog/:username` 접속 → 프로필 + 포스트 목록 표시
- [ ] PostEditor에서 태그 입력 → 발행 → PostDetail에 태그 표시
- [ ] PostEditor에서 카테고리 선택 → 발행 → PostList 카테고리 필터 작동
- [ ] Admin → 카테고리 3단 초과 생성 시 400 에러 확인
- [ ] 마이그레이션 파일 2개 (태그, 카테고리) git 커밋 완료
- [ ] `/blog/new` 경로 충돌 없음 (username이 'new'인 사용자 없음 가정)

---

## 참고: Sprint 3 준비 사항

- `blogs` 테이블 분리 (현재 User.username 직접 사용)
- `GET /api/posts?author=:username&category_id=:id` 복합 필터 최적화
- TagPostList 페이지 (`/tags/:slug`) 별도 구현
- Fulltext 검색 인덱스 추가
