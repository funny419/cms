# Sprint 1 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Admin 댓글 승인 UI, 임시저장(localStorage), 프로필 커스터마이징(bio/avatar_url), 포스트 공개범위(visibility) 4개 기능을 순서대로 구현한다.

**Architecture:** DB 마이그레이션이 필요한 기능(프로필/공개범위)은 schema 변경 → Flask-Migrate → BE API 확장 → FE 순으로 진행. Admin 댓글 승인과 임시저장은 마이그레이션 불필요. 모든 BE 변경은 TDD(pytest)로 검증.

**Tech Stack:** Python 3.11 + Flask + SQLAlchemy 3.x + Flask-Migrate + pytest / React 19 + Vite + axios + localStorage

---

## 파일 변경 맵

### 신규 생성
| 파일 | 역할 |
|------|------|
| `backend/tests/__init__.py` | pytest 패키지 인식 |
| `backend/tests/conftest.py` | pytest fixtures (app, client, admin_headers, editor_headers) |
| `backend/tests/test_admin_comments.py` | 댓글 승인/거절 API 테스트 |
| `backend/tests/test_profile.py` | bio/avatar_url 업데이트 테스트 |
| `backend/tests/test_visibility.py` | visibility 필터링 테스트 |

### 수정
| 파일 | 변경 내용 |
|------|---------|
| `backend/models/schema.py` | User에 bio/avatar_url, Post에 visibility 추가 |
| `backend/api/admin.py` | 댓글 approve/reject 엔드포인트 추가 |
| `backend/api/auth.py` | update_me()에 bio/avatar_url 처리, to_dict() 확장 |
| `backend/api/posts.py` | create/update_post에 visibility, list_posts에 visibility 필터 |
| `frontend/src/api/comments.js` | approveComment, rejectComment 함수 추가 |
| `frontend/src/pages/admin/AdminComments.jsx` | 승인/거절 버튼 추가 |
| `frontend/src/pages/PostEditor.jsx` | visibility 셀렉트 + localStorage 자동저장 추가 |
| `frontend/src/pages/Profile.jsx` | bio textarea + avatar_url input 추가 |

---

## Chunk 1: Admin 댓글 승인

### Task 1: pytest 환경 구축 + 댓글 승인/거절 BE API

**Files:**
- Create: `backend/tests/__init__.py`
- Create: `backend/tests/conftest.py`
- Create: `backend/tests/test_admin_comments.py`
- Modify: `backend/api/admin.py`

- [ ] **Step 0: app.py에 TESTING 환경 마이그레이션 스킵 추가**

`backend/app.py`의 `with app.app_context():` 블록을 수정:
```python
with app.app_context():
    if not app.config.get("TESTING"):  # ← 테스트 환경에서는 migrate 스킵
        try:
            db_upgrade()
        except Exception as e:
            print(f"[WARNING] DB migration skipped: {e}")
            print("[WARNING] Run 'flask db stamp head' on the server to fix this.")
```

- [ ] **Step 1: tests 디렉토리와 conftest.py 생성**

```python
# backend/tests/__init__.py
# (빈 파일)
```

```python
# backend/tests/conftest.py
import pytest

from app import create_app
from database import db as _db


class TestConfig:
    TESTING = True  # ← app.py에서 이 값으로 flask db upgrade 스킵
    SQLALCHEMY_DATABASE_URI = "sqlite:///:memory:"
    JWT_SECRET_KEY = "test-secret-key"
    JWT_ACCESS_TOKEN_EXPIRES = False
    SQLALCHEMY_TRACK_MODIFICATIONS = False
    SECRET_KEY = "test-secret"
    STORAGE_BACKEND = "local"
    UPLOAD_FOLDER = "/tmp/cms_test_uploads"
    MAX_CONTENT_LENGTH = 10 * 1024 * 1024


@pytest.fixture(scope="session")
def app():
    app = create_app(TestConfig)
    with app.app_context():
        _db.create_all()  # 마이그레이션 대신 직접 테이블 생성
        yield app
        _db.drop_all()


@pytest.fixture(scope="function", autouse=True)
def clean_db(app):
    with app.app_context():
        yield
        _db.session.rollback()
        for table in reversed(_db.metadata.sorted_tables):
            _db.session.execute(table.delete())
        _db.session.commit()


@pytest.fixture(scope="function")
def client(app):
    return app.test_client()


@pytest.fixture(scope="function")
def admin_headers(client, app):
    with app.app_context():
        from models.schema import User
        user = User(username="admin_user", email="admin@test.com", role="admin")
        user.set_password("testpass123")
        _db.session.add(user)
        _db.session.commit()
    res = client.post("/api/auth/login", json={"username": "admin_user", "password": "testpass123"})
    token = res.get_json()["data"]["access_token"]
    return {"Authorization": f"Bearer {token}"}


@pytest.fixture(scope="function")
def editor_headers(client, app):
    with app.app_context():
        from models.schema import User
        user = User(username="editor_user", email="editor@test.com", role="editor")
        user.set_password("testpass123")
        _db.session.add(user)
        _db.session.commit()
    res = client.post("/api/auth/login", json={"username": "editor_user", "password": "testpass123"})
    token = res.get_json()["data"]["access_token"]
    return {"Authorization": f"Bearer {token}"}


def make_comment(app, status="pending"):
    """테스트용 댓글 생성 헬퍼. app context 내부에서 호출할 것."""
    from models.schema import Comment, Post, User
    author = User(username=f"author_{status}", email=f"auth_{status}@test.com", role="editor")
    author.set_password("pass")
    _db.session.add(author)
    _db.session.flush()
    post = Post(title="Test Post", slug="test-post", author_id=author.id, status="published")
    _db.session.add(post)
    _db.session.flush()
    comment = Comment(
        post_id=post.id,
        author_name="Guest",
        author_email="guest@test.com",
        content="테스트 댓글",
        status=status,
    )
    _db.session.add(comment)
    _db.session.commit()
    return comment.id
```

- [ ] **Step 2: 댓글 승인/거절 실패 테스트 작성**

```python
# backend/tests/test_admin_comments.py
import pytest


def test_approve_pending_comment(client, app, admin_headers):
    with app.app_context():
        from tests.conftest import make_comment
        comment_id = make_comment(app, status="pending")
    res = client.put(f"/api/admin/comments/{comment_id}/approve", headers=admin_headers)
    assert res.status_code == 200
    data = res.get_json()
    assert data["success"] is True
    assert data["data"]["status"] == "approved"


def test_reject_pending_comment(client, app, admin_headers):
    with app.app_context():
        from tests.conftest import make_comment
        comment_id = make_comment(app, status="pending")
    res = client.put(f"/api/admin/comments/{comment_id}/reject", headers=admin_headers)
    assert res.status_code == 200
    data = res.get_json()
    assert data["success"] is True
    assert data["data"]["status"] == "spam"


def test_approve_requires_admin(client, editor_headers):
    res = client.put("/api/admin/comments/999/approve", headers=editor_headers)
    assert res.status_code == 403


def test_approve_not_found(client, admin_headers):
    res = client.put("/api/admin/comments/99999/approve", headers=admin_headers)
    assert res.status_code == 404


def test_reject_not_found(client, admin_headers):
    res = client.put("/api/admin/comments/99999/reject", headers=admin_headers)
    assert res.status_code == 404
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
docker compose exec backend pytest tests/test_admin_comments.py -v
```
예상: `FAILED` — `404 Not Found` (엔드포인트 미존재)

- [ ] **Step 4: admin.py에 approve/reject 엔드포인트 추가**

`backend/api/admin.py` 파일 맨 끝 (213번째 줄 이후)에 추가:

```python
@admin_bp.route("/comments/<int:comment_id>/approve", methods=["PUT"])
@roles_required("admin")
def admin_approve_comment(comment_id: int) -> tuple:
    """게스트 댓글 승인 — pending → approved."""
    comment: Comment | None = db.session.get(Comment, comment_id)
    if not comment:
        return jsonify({"success": False, "data": {}, "error": "Comment not found"}), 404
    comment.status = "approved"
    try:
        db.session.commit()
    except Exception:
        db.session.rollback()
        return jsonify({"success": False, "data": {}, "error": "An internal error occurred."}), 500
    return jsonify({"success": True, "data": {"id": comment.id, "status": comment.status}, "error": ""}), 200


@admin_bp.route("/comments/<int:comment_id>/reject", methods=["PUT"])
@roles_required("admin")
def admin_reject_comment(comment_id: int) -> tuple:
    """댓글 스팸 처리 — pending/approved → spam."""
    comment: Comment | None = db.session.get(Comment, comment_id)
    if not comment:
        return jsonify({"success": False, "data": {}, "error": "Comment not found"}), 404
    comment.status = "spam"
    try:
        db.session.commit()
    except Exception:
        db.session.rollback()
        return jsonify({"success": False, "data": {}, "error": "An internal error occurred."}), 500
    return jsonify({"success": True, "data": {"id": comment.id, "status": comment.status}, "error": ""}), 200
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
docker compose exec backend pytest tests/test_admin_comments.py -v
```
예상: 5개 모두 `PASSED`

- [ ] **Step 6: ruff + mypy 확인**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/admin.py
```
예상: 에러 없음

- [ ] **Step 7: 커밋**

```bash
git add backend/tests/__init__.py backend/tests/conftest.py \
        backend/tests/test_admin_comments.py backend/api/admin.py
git commit -m "feat: admin 댓글 승인/거절 API + pytest 환경 구축"
```

---

### Task 2: Admin 댓글 승인 FE

**Files:**
- Modify: `frontend/src/api/comments.js`
- Modify: `frontend/src/pages/admin/AdminComments.jsx`

- [ ] **Step 1: comments.js에 API 함수 추가**

`frontend/src/api/comments.js` 파일 끝에 추가:

```js
export const approveComment = async (token, commentId) => {
  try {
    const response = await axios.put(
      `/api/admin/comments/${commentId}/approve`,
      {},
      { headers: { Authorization: `Bearer ${token}` } }
    );
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '승인에 실패했습니다.' };
  }
};

export const rejectComment = async (token, commentId) => {
  try {
    const response = await axios.put(
      `/api/admin/comments/${commentId}/reject`,
      {},
      { headers: { Authorization: `Bearer ${token}` } }
    );
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '스팸 처리에 실패했습니다.' };
  }
};
```

- [ ] **Step 2: AdminComments.jsx 수정 — import 추가 및 상태 추가**

파일 상단 import 라인 수정:
```js
// 기존:
import { listAllComments, deleteComment } from '../../api/comments';
// 변경:
import { listAllComments, deleteComment, approveComment, rejectComment } from '../../api/comments';
```

`deletedIds` useState 아래에 추가:
```js
const [updatedStatuses, setUpdatedStatuses] = useState({}); // { [id]: 'approved' | 'spam' }
```

- [ ] **Step 3: 핸들러 함수 추가**

`handleDelete` 함수 아래에 추가:
```js
const handleApprove = async (commentId) => {
  const res = await approveComment(token, commentId);
  if (res.success) setUpdatedStatuses((prev) => ({ ...prev, [commentId]: 'approved' }));
  else alert(res.error);
};

const handleReject = async (commentId) => {
  const res = await rejectComment(token, commentId);
  if (res.success) setUpdatedStatuses((prev) => ({ ...prev, [commentId]: 'spam' }));
  else alert(res.error);
};
```

- [ ] **Step 4: 댓글 목록 렌더링에서 status 반영 및 버튼 추가**

`const comments = items.filter(...)` 라인 아래에 추가:
```js
const visibleComments = comments.map((c) =>
  updatedStatuses[c.id] ? { ...c, status: updatedStatuses[c.id] } : c
);
```

그리고 `comments.map` → `visibleComments.map` 으로 교체.

마지막 `<td>` (삭제 버튼 셀) 내용을 아래로 교체:
```jsx
<td style={{ padding: '10px 12px', display: 'flex', gap: 6, flexWrap: 'nowrap' }}>
  {(comment.status === 'pending') && (
    <>
      <button
        className="btn btn-primary"
        style={{ fontSize: 12, padding: '3px 10px' }}
        onClick={() => handleApprove(comment.id)}
      >
        승인
      </button>
      <button
        className="btn btn-ghost"
        style={{ fontSize: 12, padding: '3px 10px' }}
        onClick={() => handleReject(comment.id)}
      >
        스팸
      </button>
    </>
  )}
  <button
    className="btn btn-danger"
    style={{ fontSize: 12, padding: '3px 10px' }}
    onClick={() => handleDelete(comment.id)}
  >
    삭제
  </button>
</td>
```

- [ ] **Step 5: 동작 확인**

```bash
docker compose up -d
```
브라우저에서 `/admin/comments` 접속 → pending 댓글에 승인/스팸 버튼 표시 확인. 클릭 시 상태 배지 즉시 변경 확인.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/api/comments.js frontend/src/pages/admin/AdminComments.jsx
git commit -m "feat: admin 댓글 승인/스팸 처리 UI"
```

---

## Chunk 2: 임시저장 + 프로필 커스터마이징

### Task 3: 임시저장 (PostEditor localStorage)

**Files:**
- Modify: `frontend/src/pages/PostEditor.jsx`

- [ ] **Step 1: DRAFT_KEY 상수 및 자동 로드 useEffect 추가**

파일 상단 상수 선언부 (`const getUser = ...` 위)에 추가:
```js
const DRAFT_KEY = 'cms_post_draft';
```

기존 `useEffect(() => { if (!token) ...}, [id])` 블록 **바로 아래**에 추가:
```js
// 신규 작성 시만: localStorage 드래프트 복원
useEffect(() => {
  if (isEdit) return;
  const saved = localStorage.getItem(DRAFT_KEY);
  if (!saved) return;
  try {
    const draft = JSON.parse(saved);
    setForm((prev) => ({ ...prev, ...draft }));
  } catch {
    // 손상된 draft 무시
  }
}, []); // eslint-disable-line react-hooks/exhaustive-deps
```

- [ ] **Step 2: 자동저장 useEffect 추가**

위 useEffect 바로 아래:
```js
// 신규 작성 시: 10초마다 자동저장
useEffect(() => {
  if (isEdit) return;
  const timer = setInterval(() => {
    if (form.title || form.content) {
      localStorage.setItem(DRAFT_KEY, JSON.stringify(form));
    }
  }, 10000);
  return () => clearInterval(timer);
}, [form, isEdit]);
```

- [ ] **Step 3: 저장 성공 시 draft 삭제**

`handleSave` 함수의 `if (result.success)` 블록:
```js
if (result.success) {
  localStorage.removeItem(DRAFT_KEY);  // ← 추가
  navigate(`/posts/${result.data.id}`);
}
```

- [ ] **Step 4: 자동저장 인디케이터 추가**

`[saving, setSaving]` useState 아래에 추가:
```js
const [draftSaved, setDraftSaved] = useState(false);
```

자동저장 useEffect의 `localStorage.setItem` 호출 뒤에 추가:
```js
localStorage.setItem(DRAFT_KEY, JSON.stringify(form));
setDraftSaved(true);
setTimeout(() => setDraftSaved(false), 2000);
```

상단 버튼 영역의 `{error && ...}` **아래**에 추가 (error보다 낮은 우선순위):
```jsx
{error && <div className="alert alert-error" style={{ marginBottom: 16 }}>{error}</div>}
{draftSaved && !isEdit && (
  <div style={{ fontSize: 12, color: 'var(--text-light)', textAlign: 'right', marginBottom: 8 }}>
    ✓ 임시저장됨
  </div>
)}
```
> ⚠️ error 아래에 배치해야 에러 알림이 가려지지 않음 (FE 검토 지적사항)

- [ ] **Step 5: 동작 확인**

브라우저에서 `/posts/new` → 제목 입력 → 10초 대기 → "임시저장됨" 표시 확인. 페이지 새로고침 → 내용 복원 확인. 발행 후 새 글 작성 화면 → 이전 draft 미복원 확인.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/pages/PostEditor.jsx
git commit -m "feat: 포스트 에디터 10초 자동저장 (localStorage)"
```

---

### Task 4: 프로필 커스터마이징 DB 마이그레이션 + BE

**Files:**
- Modify: `backend/models/schema.py`
- Modify: `backend/api/auth.py`
- Create: `backend/tests/test_profile.py`
- Auto-create: `backend/migrations/versions/xxxx_add_user_bio_avatar.py`

- [ ] **Step 1: 테스트 먼저 작성**

```python
# backend/tests/test_profile.py
def test_update_bio(client, app, editor_headers):
    res = client.put(
        "/api/auth/me",
        json={"bio": "안녕하세요, 저는 블로거입니다."},
        headers=editor_headers,
    )
    assert res.status_code == 200
    data = res.get_json()
    assert data["success"] is True
    assert data["data"]["bio"] == "안녕하세요, 저는 블로거입니다."


def test_update_avatar_url(client, app, editor_headers):
    res = client.put(
        "/api/auth/me",
        json={"avatar_url": "/uploads/my_avatar.jpg"},
        headers=editor_headers,
    )
    assert res.status_code == 200
    data = res.get_json()
    assert data["success"] is True
    assert data["data"]["avatar_url"] == "/uploads/my_avatar.jpg"


def test_me_response_includes_bio_avatar(client, app, editor_headers):
    res = client.get("/api/auth/me", headers=editor_headers)
    assert res.status_code == 200
    data = res.get_json()["data"]
    assert "bio" in data
    assert "avatar_url" in data
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
docker compose exec backend pytest tests/test_profile.py -v
```
예상: `FAILED` — `bio` 키 없음 또는 422 에러

- [ ] **Step 3: schema.py User 모델에 bio/avatar_url 추가**

`backend/models/schema.py`의 `User` 클래스, `created_at` 컬럼 아래에 추가:
```python
bio: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
avatar_url: Mapped[Optional[str]] = mapped_column(String(500), nullable=True)
```

`User.to_dict()` 반환 dict에 추가:
```python
"bio": self.bio,
"avatar_url": self.avatar_url,
```

- [ ] **Step 4: auth.py update_me()에 bio/avatar_url 처리 추가**

`backend/api/auth.py`의 `update_me()` 함수, `if "email" in data:` 블록 아래에 추가:
```python
if "bio" in data:
    user.bio = data["bio"]
if "avatar_url" in data:
    user.avatar_url = data["avatar_url"]
```

- [ ] **Step 5: 테스트 통과 확인 (in-memory SQLite)**

```bash
docker compose exec backend pytest tests/test_profile.py -v
```
예상: 3개 `PASSED`

- [ ] **Step 6: Flask-Migrate로 실제 DB 마이그레이션 생성**

```bash
docker compose exec backend flask db migrate -m "add user bio avatar_url"
```
예상: `migrations/versions/xxxx_add_user_bio_avatar_url.py` 생성

- [ ] **Step 7: 마이그레이션 파일 검토 후 백엔드 재시작**

생성된 마이그레이션 파일을 열어 `op.add_column('users', sa.Column('bio', sa.Text(), nullable=True))` 등 올바른 컬럼이 추가됐는지 확인.

```bash
docker compose restart backend
docker compose logs -f backend | head -20
```
예상: `flask db upgrade` 자동 실행, 에러 없음

- [ ] **Step 8: ruff + mypy 확인**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/auth.py models/schema.py
```

- [ ] **Step 9: 커밋**

```bash
git add backend/models/schema.py backend/api/auth.py \
        backend/tests/test_profile.py \
        backend/migrations/versions/
git commit -m "feat: 프로필 bio/avatar_url 필드 추가 (DB 마이그레이션 + API)"
```

---

### Task 5: 프로필 커스터마이징 FE

**Files:**
- Modify: `frontend/src/pages/Profile.jsx`

- [ ] **Step 1: formData에 bio/avatar_url 추가**

`useState` 초기값도 함께 변경:
```js
// 기존:
const [formData, setFormData] = useState({ email: '', password: '' });
// 변경:
const [formData, setFormData] = useState({ email: '', password: '', bio: '', avatar_url: '' });
```

그리고 `setFormData` 초기화 변경:
```js
// 기존:
setFormData({ email: result.data.email, password: '' });
// 변경 (4개 필드 명시):
setFormData({
  email: result.data.email,
  password: '',
  bio: result.data.bio || '',
  avatar_url: result.data.avatar_url || '',
});
```

- [ ] **Step 2: handleSubmit에서 bio/avatar_url 변경 감지**

`updateData` 조건 블록에 추가:
```js
if (formData.bio !== (user.bio || '')) updateData.bio = formData.bio;
if (formData.avatar_url !== (user.avatar_url || '')) updateData.avatar_url = formData.avatar_url;
```

성공 시 user 상태도 업데이트:
```js
if (result.success) {
  setMessage('프로필이 저장됐습니다.');
  setUser({ ...user, ...updateData });  // ← 기존 조건부 업데이트 대신 통합
  if (updateData.password) setFormData({ ...formData, password: '' });
}
```

- [ ] **Step 3: 아바타 미리보기 + bio textarea UI 추가**

`<hr className="divider" />` 아래, `{message && ...}` 위에 추가:
```jsx
{/* 아바타 미리보기 */}
{formData.avatar_url && (
  <div style={{ marginBottom: 16, textAlign: 'center' }}>
    <img
      src={formData.avatar_url}
      alt="프로필 이미지"
      style={{ width: 80, height: 80, borderRadius: '50%', objectFit: 'cover', border: '2px solid var(--border)' }}
      onError={(e) => {
        e.target.style.display = 'none';
        setError('유효하지 않은 이미지 URL입니다.');  // ← FE 검토 지적: 에러 메시지 추가
      }}
      onLoad={() => setError('')}  // ← 성공 시 에러 초기화
    />
  </div>
)}
```

`<form>` 안, 이메일 필드 위에 추가:
```jsx
<div className="form-group">
  <label className="form-label" htmlFor="avatar_url">프로필 이미지 URL</label>
  <input
    className="form-input"
    type="url"
    id="avatar_url"
    name="avatar_url"
    value={formData.avatar_url}
    onChange={handleChange}
    placeholder="https://... 또는 /uploads/..."
  />
</div>

<div className="form-group">
  <label className="form-label" htmlFor="bio">자기소개</label>
  <textarea
    className="form-input"
    id="bio"
    name="bio"
    value={formData.bio}
    onChange={handleChange}
    placeholder="간단한 자기소개를 입력하세요"
    rows={3}
    style={{ resize: 'vertical' }}
  />
</div>
```

- [ ] **Step 4: 동작 확인**

브라우저에서 `/profile` 접속 → avatar_url 입력 시 미리보기 표시 확인 → bio 입력 → 저장 → 새로고침 후 값 유지 확인 (`GET /api/auth/me` 응답에 bio/avatar_url 포함 확인).

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/pages/Profile.jsx
git commit -m "feat: 프로필 bio/avatar_url 편집 UI"
```

---

## Chunk 3: 포스트 공개범위 (visibility)

### Task 6: visibility DB 마이그레이션 + BE API

**Files:**
- Modify: `backend/models/schema.py`
- Modify: `backend/api/posts.py`
- Create: `backend/tests/test_visibility.py`
- Auto-create: `backend/migrations/versions/xxxx_add_post_visibility.py`

- [ ] **Step 1: 테스트 먼저 작성**

```python
# backend/tests/test_visibility.py
def make_post(app, status="published", visibility="public", author_role="editor"):
    from models.schema import Post, User
    from database import db
    user = User(
        username=f"u_{visibility}_{author_role}",
        email=f"{visibility}_{author_role}@test.com",
        role=author_role,
    )
    user.set_password("pass")
    db.session.add(user)
    db.session.flush()
    post = Post(
        title=f"Post {visibility}",
        slug=f"post-{visibility}",
        author_id=user.id,
        status=status,
        visibility=visibility,
    )
    db.session.add(post)
    db.session.commit()
    return post.id


def test_public_post_visible_to_guest(client, app):
    with app.app_context():
        make_post(app, visibility="public")
    res = client.get("/api/posts")
    assert res.status_code == 200
    data = res.get_json()["data"]
    assert any(p["visibility"] == "public" for p in data["items"])


def test_private_post_not_visible_to_guest(client, app):
    with app.app_context():
        make_post(app, visibility="private")
    res = client.get("/api/posts")
    assert res.status_code == 200
    items = res.get_json()["data"]["items"]
    assert not any(p["visibility"] == "private" for p in items)


def test_members_only_not_visible_to_guest(client, app):
    with app.app_context():
        make_post(app, visibility="members_only")
    res = client.get("/api/posts")
    assert res.status_code == 200
    items = res.get_json()["data"]["items"]
    assert not any(p["visibility"] == "members_only" for p in items)


def test_members_only_visible_to_editor(client, app, editor_headers):
    with app.app_context():
        make_post(app, visibility="members_only")
    res = client.get("/api/posts", headers=editor_headers)
    assert res.status_code == 200
    items = res.get_json()["data"]["items"]
    assert any(p["visibility"] == "members_only" for p in items)


def test_post_created_with_visibility(client, app, editor_headers):
    res = client.post(
        "/api/posts",
        json={"title": "Members Only Post", "visibility": "members_only", "status": "published"},
        headers=editor_headers,
    )
    assert res.status_code == 201
    data = res.get_json()["data"]
    assert data["visibility"] == "members_only"
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
docker compose exec backend pytest tests/test_visibility.py -v
```
예상: `FAILED` — `visibility` 컬럼 없음 또는 AttributeError

- [ ] **Step 3: schema.py Post 모델에 visibility 추가**

`backend/models/schema.py`의 `Post` 클래스, `content_format` 컬럼 아래에 추가:
```python
# visibility: 접근 권한 차원 (status=발행상태와 별개)
# public: 비로그인도 조회 가능
# members_only: 로그인 사용자만 조회 가능
# private: 작성자 + admin만 조회 가능
visibility: Mapped[str] = mapped_column(String(20), default="public", nullable=False)
```

`Post.to_dict()` 반환 dict에 추가:
```python
"visibility": self.visibility,
```

- [ ] **Step 4: posts.py create_post()에 visibility 처리 추가**

`create_post()` 함수의 `post = Post(...)` 블록에 `visibility` 추가:
```python
post = Post(
    title=data["title"],
    slug=data.get("slug", ""),
    content=data.get("content", ""),
    excerpt=data.get("excerpt", ""),
    status=data.get("status", "draft"),
    post_type=data.get("post_type", "post"),
    content_format=content_format,
    author_id=author_id,
    visibility=data.get("visibility", "public"),  # ← 추가
)
```

- [ ] **Step 5: posts.py update_post()의 field 목록에 visibility 추가**

```python
# 기존:
for field in ("title", "slug", "content", "excerpt", "status", "post_type", "content_format"):
# 변경:
for field in ("title", "slug", "content", "excerpt", "status", "post_type", "content_format", "visibility"):
```

그리고 content_format 유효성 검사 블록 아래에 추가:
```python
if field == "visibility" and data[field] not in ("public", "members_only", "private"):
    continue
```

- [ ] **Step 6: posts.py 상단 import에 or_ 추가**

> ⚠️ BE 검토 지적: `or_`가 현재 posts.py에 없음 — 먼저 추가 필수.

`backend/api/posts.py` 3번째 줄 수정:
```python
# 기존:
from sqlalchemy import func, select
# 변경:
from sqlalchemy import func, or_, select
```

- [ ] **Step 7: list_posts()에 visibility 필터 추가**

`base_query = (select(...).where(Post.status == "published"))` 블록 아래에 추가:

> ⚠️ BE 검토 지적: `total_query`에도 반드시 동일한 필터 적용. 미적용 시 페이지네이션 `has_more` 오류.

```python
# visibility 필터: 로그인 여부에 따라 접근 가능한 포스트만 조회
if current_user_id is None:
    # 비로그인: public만
    base_query = base_query.where(Post.visibility == "public")
    total_query = total_query.where(Post.visibility == "public")  # ← total_query 필수
else:
    # 로그인: public + members_only (private는 본인 + admin만)
    _user = db.session.get(User, current_user_id)
    if _user and _user.role == "admin":
        pass  # admin은 전체 조회
    else:
        _vis_filter = or_(
            Post.visibility == "public",
            Post.visibility == "members_only",
            (Post.visibility == "private") & (Post.author_id == current_user_id),
        )
        base_query = base_query.where(_vis_filter)
        total_query = total_query.where(_vis_filter)  # ← total_query 필수
```

- [ ] **Step 8: get_post()에 visibility 접근 제어 추가**

> ⚠️ BE 검토 지적: private 글 URL 직접 접근 시 내용 노출 — 보안 취약점.

`get_post()` 함수에서 `if not post: return 404` 바로 아래, `if not skip_count:` 바로 위에 삽입:

```python
# visibility 접근 제어 (URL 직접 접근 보호)
if post.visibility == "members_only" and current_user_id is None:
    return jsonify({"success": False, "data": {}, "error": "로그인이 필요합니다."}), 403
elif post.visibility == "private":
    if current_user_id is None:
        return jsonify({"success": False, "data": {}, "error": "접근 권한이 없습니다."}), 403
    _viewer: User | None = db.session.get(User, current_user_id)
    if _viewer and _viewer.role != "admin" and post.author_id != current_user_id:
        return jsonify({"success": False, "data": {}, "error": "접근 권한이 없습니다."}), 403
```

- [ ] **Step 9: 테스트 통과 확인 + get_post 보호 테스트 추가**

```bash
docker compose exec backend pytest tests/test_visibility.py -v
```
예상: 5개 `PASSED`

`test_visibility.py`에 추가:
```python
def test_private_post_blocked_for_guest(client, app):
    with app.app_context():
        post_id = make_post(app, visibility="private")
    res = client.get(f"/api/posts/{post_id}")
    assert res.status_code == 403
```
추가 후 재실행: 6개 `PASSED`

- [ ] **Step 10: 전체 테스트 회귀 확인**

```bash
docker compose exec backend pytest tests/ -v
```
예상: 전체 통과

- [ ] **Step 11: Flask-Migrate로 마이그레이션 생성**

```bash
docker compose exec backend flask db migrate -m "add post visibility"
```
예상: `migrations/versions/xxxx_add_post_visibility.py` 생성

생성된 파일에서 아래 내용 확인:
```python
op.add_column('posts', sa.Column('visibility', sa.String(length=20), nullable=False, server_default='public'))
```
`server_default='public'`이 없으면 수동으로 추가할 것 (기존 rows NULL 방지).

- [ ] **Step 13: 백엔드 재시작 + 로그 확인**

```bash
docker compose restart backend
docker compose logs -f backend | head -20
```

- [ ] **Step 14: ruff + mypy 확인**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/posts.py models/schema.py
```

- [ ] **Step 15: 커밋**

```bash
git add backend/models/schema.py backend/api/posts.py \
        backend/tests/test_visibility.py \
        backend/migrations/versions/
git commit -m "feat: 포스트 공개범위 visibility 컬럼 + API 필터링 + 단건 접근 제어"
```

---

### Task 7: 포스트 공개범위 FE

**Files:**
- Modify: `frontend/src/pages/PostEditor.jsx`

- [ ] **Step 1: form 상태에 visibility 추가**

`useState` 초기값에 추가:
```js
const [form, setForm] = useState({
  title: '',
  content: '',
  excerpt: '',
  slug: '',
  post_type: 'post',
  content_format: 'html',
  visibility: 'public',  // ← 추가
});
```

- [ ] **Step 2: 편집 모드에서 visibility 로드**

`setForm({...})` 블록에 추가:
```js
setForm({
  title: res.data.title || '',
  content: res.data.content || '',
  excerpt: res.data.excerpt || '',
  slug: res.data.slug || '',
  post_type: res.data.post_type || 'post',
  content_format: res.data.content_format || 'html',
  visibility: res.data.visibility || 'public',  // ← 추가
});
```

- [ ] **Step 3: 하단 옵션 grid에 visibility 셀렉트 추가**

하단 옵션 영역의 `gridTemplateColumns` 값을 반응형으로 변경:

```jsx
// 기존:
gridTemplateColumns: '1fr 1fr 120px',
// 변경 (FE 검토 지적: 4열 고정 시 모바일 깨짐):
gridTemplateColumns: 'repeat(auto-fit, minmax(120px, 1fr))',
```

그리고 타입 `<select>` 아래에 추가:

```jsx
<div>
  <label className="form-label">공개 범위</label>
  <select
    className="form-input"
    name="visibility"
    value={form.visibility}
    onChange={handleChange}
  >
    <option value="public">전체 공개</option>
    <option value="members_only">회원만</option>
    <option value="private">나만 보기</option>
  </select>
</div>
```

- [ ] **Step 4: 동작 확인**

브라우저에서 `/posts/new` → 공개범위 "나만 보기" 선택 → 발행 → `/posts` 목록에서 비로그인 시 미표시, 로그인 시 자신의 글 표시 확인.

편집 화면(`/posts/:id/edit`) → 기존 공개범위 값이 셀렉트에 표시되는지 확인.

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/pages/PostEditor.jsx
git commit -m "feat: 포스트 에디터 공개범위 선택 UI"
```

---

## Sprint 1 완료 체크리스트

- [ ] `docker compose exec backend pytest tests/ -v` — 전체 통과
- [ ] `docker compose exec backend ruff check .` — 에러 없음
- [ ] `docker compose exec backend mypy .` — 에러 없음
- [ ] Admin 대시보드 → 댓글 관리 → pending 댓글에 승인/스팸 버튼 표시
- [ ] 신규 포스트 작성 → 10초 후 "임시저장됨" 표시, 새로고침 후 복원
- [ ] 프로필 페이지 → bio 입력 + avatar_url 입력 → 저장 → 새로고침 유지
- [ ] 포스트 에디터 → 공개범위 선택 → 발행 → 비로그인/로그인 필터 정상 동작
- [ ] 마이그레이션 파일 2개 git 커밋 완료
