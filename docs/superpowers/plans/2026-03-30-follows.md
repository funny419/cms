# 구독/이웃 (Follows) Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 다른 블로거를 팔로우/언팔로우하고, 팔로우한 블로거의 최신 포스트를 `/feed` 페이지에서 모아볼 수 있게 한다.

**Architecture:** `follows` 테이블(follower_id↔following_id)을 신규 생성. `backend/api/follows.py` Blueprint에 팔로우/언팔로우/팔로워목록/팔로잉목록/피드 API 구현. 프로필 응답(`GET /api/auth/users/:username`)에 `follower_count`, `following_count`, `is_following` 추가. FE는 BlogHome ProfileCard에 팔로우 버튼, `/feed` 페이지(이웃 피드) 추가.

**Tech Stack:** Python 3.11 + Flask + SQLAlchemy 3.x + Flask-Migrate + pytest(MariaDB) / React 19 + Vite + axios

---

## 파일 변경 맵

### 신규 생성
| 파일 | 역할 |
|------|------|
| `backend/api/follows.py` | 팔로우 관련 API Blueprint |
| `backend/tests/test_follows.py` | 팔로우 API 테스트 |
| `frontend/src/pages/Feed.jsx` | `/feed` 이웃 피드 페이지 |
| `backend/migrations/versions/xxxx_create_follows.py` | 마이그레이션 |

### 수정
| 파일 | 변경 내용 |
|------|---------|
| `backend/models/schema.py` | Follow 모델 추가, User.followers/following relationship |
| `backend/api/auth.py` | get_user_profile() 응답에 follower_count/following_count/is_following 추가 |
| `backend/app.py` | follows_bp 등록 |
| `frontend/src/api/users.js` | followUser, unfollowUser, getFeed 함수 추가 |
| `frontend/src/components/ProfileCard.jsx` | 팔로우 버튼 + 팔로워/팔로잉 수 표시 |
| `frontend/src/pages/BlogHome.jsx` | ProfileCard에 onFollow prop 전달 |
| `frontend/src/App.jsx` | `/feed` 라우트 추가 |
| `frontend/src/components/Nav.jsx` | editor에 "피드" 링크 추가 |

---

## Chunk 1: BE — Follow 스키마 + API

### Task 1: follows 스키마 + 마이그레이션

**Files:**
- Modify: `backend/models/schema.py`
- Auto-create: `backend/migrations/versions/xxxx_create_follows.py`

- [ ] **Step 1: schema.py에 Follow 모델 추가**

`backend/models/schema.py` 파일을 읽고, 파일 끝 (Category 클래스 아래)에 추가:

```python
class Follow(Base):
    """팔로우 관계 (이웃)"""

    __tablename__ = "follows"

    id: Mapped[int] = mapped_column(primary_key=True)
    follower_id: Mapped[int] = mapped_column(
        ForeignKey("users.id", ondelete="CASCADE"), nullable=False
    )
    following_id: Mapped[int] = mapped_column(
        ForeignKey("users.id", ondelete="CASCADE"), nullable=False
    )
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())

    __table_args__ = (
        UniqueConstraint("follower_id", "following_id", name="uq_follow"),
    )
```

User 클래스에 relationship 추가 (`comments` relationship 아래):
```python
followers: Mapped[List["Follow"]] = relationship(
    "Follow",
    foreign_keys="[Follow.following_id]",
    back_populates="following_user",
    cascade="all, delete-orphan",
)
followings: Mapped[List["Follow"]] = relationship(
    "Follow",
    foreign_keys="[Follow.follower_id]",
    back_populates="follower_user",
    cascade="all, delete-orphan",
)
```

Follow 클래스에 relationship 추가:
```python
follower_user: Mapped["User"] = relationship(
    "User", foreign_keys=[follower_id], back_populates="followings"
)
following_user: Mapped["User"] = relationship(
    "User", foreign_keys=[following_id], back_populates="followers"
)
```

- [ ] **Step 2: 마이그레이션 생성**

```bash
docker compose exec backend flask db migrate -m "create follows table"
```

생성된 파일 확인:
- `follows` 테이블 생성
- `uq_follow` UNIQUE constraint (follower_id, following_id)
- `follower_id`, `following_id` 각각 INDEX (FK에 자동 생성)

- [ ] **Step 3: 백엔드 재시작 + 전체 테스트 회귀**

```bash
docker compose restart backend
sleep 5
docker compose exec backend pytest tests/ -v 2>&1 | tail -5
```
예상: 전체 통과

- [ ] **Step 4: mypy**

```bash
docker compose exec backend mypy models/schema.py
```

- [ ] **Step 5: 커밋**

```bash
git add backend/models/schema.py backend/migrations/versions/
git commit -m "feat: follows 테이블 스키마 + 마이그레이션"
```

---

### Task 2: follows.py API + 테스트

**Files:**
- Create: `backend/api/follows.py`
- Create: `backend/tests/test_follows.py`
- Modify: `backend/app.py`

- [ ] **Step 1: 테스트 먼저 작성**

```python
# backend/tests/test_follows.py
import uuid
from database import db as _db


def make_user(role="editor"):
    from models.schema import User
    uname = f"u_{uuid.uuid4().hex[:6]}"
    user = User(username=uname, email=f"{uname}@t.com", role=role)
    user.set_password("pass")
    _db.session.add(user)
    _db.session.commit()
    return user.id, uname


def get_token(client, username):
    res = client.post("/api/auth/login", json={"username": username, "password": "pass"})
    return res.get_json()["data"]["access_token"]


def test_follow_user(client, app):
    with app.app_context():
        uid1, u1 = make_user()
        uid2, u2 = make_user()
    token1 = get_token(client, u1)
    res = client.post(f"/api/users/{u2}/follow", headers={"Authorization": f"Bearer {token1}"})
    assert res.status_code == 201
    assert res.get_json()["data"]["following"] is True


def test_unfollow_user(client, app):
    with app.app_context():
        uid1, u1 = make_user()
        uid2, u2 = make_user()
    token1 = get_token(client, u1)
    client.post(f"/api/users/{u2}/follow", headers={"Authorization": f"Bearer {token1}"})
    res = client.delete(f"/api/users/{u2}/follow", headers={"Authorization": f"Bearer {token1}"})
    assert res.status_code == 200
    assert res.get_json()["data"]["following"] is False


def test_cannot_follow_self(client, app):
    with app.app_context():
        uid1, u1 = make_user()
    token1 = get_token(client, u1)
    res = client.post(f"/api/users/{u1}/follow", headers={"Authorization": f"Bearer {token1}"})
    assert res.status_code == 400


def test_follow_requires_login(client, app):
    with app.app_context():
        _, u2 = make_user()
    res = client.post(f"/api/users/{u2}/follow")
    assert res.status_code == 401


def test_get_feed_empty(client, app, editor_headers):
    res = client.get("/api/feed", headers=editor_headers)
    assert res.status_code == 200
    data = res.get_json()["data"]
    assert "items" in data
    assert data["total"] == 0


def test_get_feed_with_follows(client, app):
    with app.app_context():
        uid1, u1 = make_user()
        uid2, u2 = make_user()
        from models.schema import Post
        post = Post(
            title="Feed Test Post",
            slug=uuid.uuid4().hex[:8],
            author_id=uid2,
            status="published",
            visibility="public",
        )
        _db.session.add(post)
        _db.session.commit()
        post_id = post.id
    token1 = get_token(client, u1)
    client.post(f"/api/users/{u2}/follow", headers={"Authorization": f"Bearer {token1}"})
    res = client.get("/api/feed", headers={"Authorization": f"Bearer {token1}"})
    assert res.status_code == 200
    items = res.get_json()["data"]["items"]
    assert any(p["id"] == post_id for p in items)
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
docker compose exec backend pytest tests/test_follows.py -v
```
예상: FAILED (엔드포인트 없음)

- [ ] **Step 3: follows.py 생성**

```python
# backend/api/follows.py
from flask import Blueprint, jsonify, request
from flask_jwt_extended import get_jwt_identity, jwt_required, verify_jwt_in_request
from sqlalchemy import func, or_, select
from sqlalchemy.exc import IntegrityError

from database import db
from models.schema import Follow, Post, User

follows_bp = Blueprint("follows", __name__, url_prefix="/api/users")


@follows_bp.route("/<username>/follow", methods=["POST"])
@jwt_required()
def follow_user(username: str) -> tuple:
    """팔로우."""
    current_user_id: int = int(get_jwt_identity())
    target: User | None = db.session.execute(
        select(User).where(User.username == username)
    ).scalar_one_or_none()

    if not target or target.role == "deactivated":
        return jsonify({"success": False, "data": {}, "error": "User not found"}), 404

    if target.id == current_user_id:
        return jsonify({"success": False, "data": {}, "error": "자신을 팔로우할 수 없습니다."}), 400

    existing = db.session.execute(
        select(Follow)
        .where(Follow.follower_id == current_user_id)
        .where(Follow.following_id == target.id)
    ).scalar_one_or_none()

    if existing:
        return jsonify({"success": True, "data": {"following": True}, "error": ""}), 200

    follow = Follow(follower_id=current_user_id, following_id=target.id)
    db.session.add(follow)
    try:
        db.session.commit()
    except IntegrityError:
        db.session.rollback()
        return jsonify({"success": True, "data": {"following": True}, "error": ""}), 200

    return jsonify({"success": True, "data": {"following": True}, "error": ""}), 201


@follows_bp.route("/<username>/follow", methods=["DELETE"])
@jwt_required()
def unfollow_user(username: str) -> tuple:
    """언팔로우."""
    current_user_id: int = int(get_jwt_identity())
    target: User | None = db.session.execute(
        select(User).where(User.username == username)
    ).scalar_one_or_none()

    if not target:
        return jsonify({"success": False, "data": {}, "error": "User not found"}), 404

    existing = db.session.execute(
        select(Follow)
        .where(Follow.follower_id == current_user_id)
        .where(Follow.following_id == target.id)
    ).scalar_one_or_none()

    if existing:
        db.session.delete(existing)
        db.session.commit()

    return jsonify({"success": True, "data": {"following": False}, "error": ""}), 200


@follows_bp.route("/<username>/followers", methods=["GET"])
def list_followers(username: str) -> tuple:
    """팔로워 목록 (나를 팔로우하는 사람)."""
    target: User | None = db.session.execute(
        select(User).where(User.username == username)
    ).scalar_one_or_none()
    if not target:
        return jsonify({"success": False, "data": {}, "error": "User not found"}), 404

    followers = db.session.execute(
        select(User)
        .join(Follow, Follow.follower_id == User.id)
        .where(Follow.following_id == target.id)
        .order_by(Follow.created_at.desc())
    ).scalars().all()

    items = [{"id": u.id, "username": u.username, "avatar_url": u.avatar_url} for u in followers]
    return jsonify({"success": True, "data": {"items": items, "total": len(items)}, "error": ""}), 200


@follows_bp.route("/<username>/following", methods=["GET"])
def list_following(username: str) -> tuple:
    """팔로잉 목록 (내가 팔로우하는 사람)."""
    target: User | None = db.session.execute(
        select(User).where(User.username == username)
    ).scalar_one_or_none()
    if not target:
        return jsonify({"success": False, "data": {}, "error": "User not found"}), 404

    following = db.session.execute(
        select(User)
        .join(Follow, Follow.following_id == User.id)
        .where(Follow.follower_id == target.id)
        .order_by(Follow.created_at.desc())
    ).scalars().all()

    items = [{"id": u.id, "username": u.username, "avatar_url": u.avatar_url} for u in following]
    return jsonify({"success": True, "data": {"items": items, "total": len(items)}, "error": ""}), 200


@follows_bp.route("", methods=["GET"])
def feed() -> tuple:
    """이웃 피드 — 팔로우한 사람들의 최신 포스트."""
    # /api/users 경로와 충돌 방지 위해 별도 Blueprint 사용 권장
    # 이 라우트는 /api/feed로 분리 예정
    return jsonify({"success": False, "data": {}, "error": "Use /api/feed"}), 400
```

> ⚠️ `/api/feed` 엔드포인트는 `/api/users` prefix와 맞지 않으므로 별도 Blueprint로 분리.

- [ ] **Step 4: app.py에 follows_bp 등록 + /api/feed 엔드포인트 추가**

`backend/app.py`를 읽고 추가:
```python
from api.follows import follows_bp
app.register_blueprint(follows_bp)
```

그리고 `app.py`에 직접 `/api/feed` 라우트 추가 (또는 `follows.py`에서 별도 Blueprint로):

`follows.py`에 피드용 Blueprint 추가:
```python
feed_bp = Blueprint("feed", __name__, url_prefix="/api")


@feed_bp.route("/feed", methods=["GET"])
@jwt_required()
def get_feed() -> tuple:
    """팔로우한 사람들의 최신 포스트 피드."""
    current_user_id: int = int(get_jwt_identity())

    page = max(1, request.args.get("page", 1, type=int) or 1)
    per_page = min(max(1, request.args.get("per_page", 20, type=int) or 20), 100)
    offset = (page - 1) * per_page

    # 팔로우 중인 사람들의 ID
    following_sq = select(Follow.following_id).where(Follow.follower_id == current_user_id)

    base = (
        select(Post)
        .where(Post.author_id.in_(following_sq))
        .where(Post.status == "published")
        .where(
            or_(
                Post.visibility == "public",
                Post.visibility == "members_only",
            )
        )
    )

    total: int = db.session.execute(
        select(func.count()).select_from(base.subquery())
    ).scalar() or 0

    posts = db.session.execute(
        base.order_by(Post.created_at.desc()).offset(offset).limit(per_page)
    ).scalars().all()

    return jsonify(
        {
            "success": True,
            "data": {
                "items": [p.to_dict() for p in posts],
                "page": page,
                "per_page": per_page,
                "total": total,
                "has_more": page * per_page < total,
            },
            "error": "",
        }
    ), 200
```

`app.py`에도 feed_bp 등록:
```python
from api.follows import feed_bp
app.register_blueprint(feed_bp)
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
docker compose exec backend pytest tests/test_follows.py -v
docker compose exec backend pytest tests/ -v 2>&1 | tail -5
```
예상: 전체 통과

- [ ] **Step 6: ruff + mypy**

```bash
docker compose exec backend ruff check .
docker compose exec backend mypy api/follows.py
```

- [ ] **Step 7: 커밋**

```bash
git add backend/api/follows.py backend/tests/test_follows.py backend/app.py
git commit -m "feat: 팔로우/언팔로우/팔로워목록/팔로잉목록/피드 API"
```

---

### Task 3: 프로필 응답에 팔로우 정보 추가

**Files:**
- Modify: `backend/api/auth.py`

- [ ] **Step 1: auth.py의 get_user_profile() 수정**

파일을 읽고 `get_user_profile()` 함수에서 `d["post_count"] = post_count` 아래에 추가:

```python
# 팔로워/팔로잉 수
follower_count: int = db.session.execute(
    select(func.count(Follow.id)).where(Follow.following_id == user.id)
).scalar() or 0

following_count: int = db.session.execute(
    select(func.count(Follow.id)).where(Follow.follower_id == user.id)
).scalar() or 0

# 현재 로그인 유저의 팔로우 여부
is_following: bool = False
try:
    verify_jwt_in_request(optional=True)
    raw_id = get_jwt_identity()
    viewer_id = int(raw_id) if raw_id else None
except Exception:
    viewer_id = None

if viewer_id and viewer_id != user.id:
    is_following = bool(
        db.session.execute(
            select(Follow)
            .where(Follow.follower_id == viewer_id)
            .where(Follow.following_id == user.id)
        ).scalar_one_or_none()
    )

d["follower_count"] = follower_count
d["following_count"] = following_count
d["is_following"] = is_following
```

import 추가 (파일 상단):
```python
from flask_jwt_extended import create_access_token, get_jwt_identity, jwt_required, verify_jwt_in_request
from models.schema import Follow, Post, User
```

- [ ] **Step 2: 전체 테스트 회귀**

```bash
docker compose exec backend pytest tests/ -v 2>&1 | tail -5
```

- [ ] **Step 3: 커밋**

```bash
git add backend/api/auth.py
git commit -m "feat: 프로필 응답에 follower_count/following_count/is_following 추가"
```

---

## Chunk 2: FE — 팔로우 버튼 + 피드 페이지

### Task 4: users.js API + ProfileCard 팔로우 버튼 + BlogHome 연동

**Files:**
- Modify: `frontend/src/api/users.js`
- Modify: `frontend/src/components/ProfileCard.jsx`
- Modify: `frontend/src/pages/BlogHome.jsx`

- [ ] **Step 1: users.js에 팔로우 API 함수 추가**

파일을 읽고 끝에 추가:
```js
export const followUser = async (token, username) => {
  try {
    const response = await axios.post(
      `/api/users/${username}/follow`,
      {},
      { headers: authHeader(token) }
    );
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '팔로우에 실패했습니다.' };
  }
};

export const unfollowUser = async (token, username) => {
  try {
    const response = await axios.delete(
      `/api/users/${username}/follow`,
      { headers: authHeader(token) }
    );
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '언팔로우에 실패했습니다.' };
  }
};

export const getFeed = async (token, page = 1, perPage = 20) => {
  try {
    const response = await axios.get('/api/feed', {
      headers: authHeader(token),
      params: { page, per_page: perPage },
    });
    return response.data;
  } catch (error) {
    return { success: false, error: error.response?.data?.error || '피드를 불러오지 못했습니다.' };
  }
};
```

- [ ] **Step 2: ProfileCard.jsx에 팔로우 버튼 추가**

파일을 읽고 props에 `onFollow`, `isFollowing`, `followerCount`, `followingCount` 추가:

```jsx
export default function ProfileCard({ user, blogColor, onFollow, isFollowing, isOwnBlog }) {
  if (!user) return null;
  // ... 기존 코드
```

메타 정보 섹션 아래에 팔로우 버튼 + 팔로워/팔로잉 수 추가:
```jsx
{/* 팔로워/팔로잉 수 */}
<div style={{ display: 'flex', gap: 16, marginTop: 8, fontSize: 13 }}>
  <span>팔로워 <strong>{user.follower_count ?? 0}</strong></span>
  <span>팔로잉 <strong>{user.following_count ?? 0}</strong></span>
</div>

{/* 팔로우 버튼 (자신 블로그가 아닐 때 + 로그인 시) */}
{!isOwnBlog && onFollow && (
  <button
    className={isFollowing ? 'btn btn-ghost' : 'btn btn-primary'}
    style={{ marginTop: 10, fontSize: 13 }}
    onClick={onFollow}
  >
    {isFollowing ? '팔로잉 ✓' : '팔로우'}
  </button>
)}
```

- [ ] **Step 3: BlogHome.jsx에 팔로우 상태 + 핸들러 추가**

파일을 읽고 수정:

import 추가:
```jsx
import { followUser, unfollowUser } from '../api/users';
```

상태 추가:
```jsx
const [isFollowing, setIsFollowing] = useState(false);
```

profile 로드 후 is_following 초기화:
```jsx
if (profileRes.success) {
  setProfile(profileRes.data);
  setIsFollowing(profileRes.data.is_following || false);
}
```

팔로우 핸들러 추가:
```jsx
const getUser = () => {
  try { return JSON.parse(localStorage.getItem('user')); } catch { return null; }
};
const currentUser = getUser();
const isOwnBlog = currentUser?.username === username;

const handleFollow = async () => {
  if (!token) { navigate('/login'); return; }
  const res = isFollowing
    ? await unfollowUser(token, username)
    : await followUser(token, username);
  if (res.success) {
    setIsFollowing(res.data.following);
    setProfile((prev) => ({
      ...prev,
      follower_count: (prev.follower_count || 0) + (res.data.following ? 1 : -1),
    }));
  }
};
```

ProfileCard에 props 전달:
```jsx
<ProfileCard
  user={profile}
  blogColor={profile?.blog_color}
  onFollow={handleFollow}
  isFollowing={isFollowing}
  isOwnBlog={isOwnBlog}
/>
```

- [ ] **Step 4: ESLint 확인**

```bash
docker compose exec -T frontend npx eslint \
  src/api/users.js src/components/ProfileCard.jsx src/pages/BlogHome.jsx 2>&1
```

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/api/users.js \
        frontend/src/components/ProfileCard.jsx \
        frontend/src/pages/BlogHome.jsx
git commit -m "feat: 팔로우/언팔로우 버튼 + 팔로워/팔로잉 수 표시"
```

---

### Task 5: /feed 페이지 + App.jsx + Nav

**Files:**
- Create: `frontend/src/pages/Feed.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/components/Nav.jsx`

- [ ] **Step 1: Feed.jsx 생성**

```jsx
// frontend/src/pages/Feed.jsx
import { useCallback } from 'react';
import { useNavigate } from 'react-router-dom';
import { getFeed } from '../api/users';
import useInfiniteScroll from '../hooks/useInfiniteScroll';
import TagCloud from '../components/widgets/TagCloud';

export default function Feed() {
  const navigate = useNavigate();
  const token = localStorage.getItem('token');

  const fetchFn = useCallback(
    (page) => {
      if (!token) return Promise.resolve({ success: false, data: { items: [], has_more: false } });
      return getFeed(token, page, 20);
    },
    [token]
  );
  const { items: posts, loading, hasMore, error, sentinelRef } = useInfiniteScroll(
    fetchFn,
    [token]
  );

  if (!token) {
    return (
      <div className="page-content">
        <div className="empty-state" style={{ marginTop: 80 }}>
          <p style={{ fontSize: 32, marginBottom: 12 }}>🏠</p>
          <p>로그인 후 이웃의 새 글을 모아볼 수 있습니다.</p>
          <button className="btn btn-primary" style={{ marginTop: 16 }} onClick={() => navigate('/login')}>
            로그인
          </button>
        </div>
      </div>
    );
  }

  return (
    <div className="page-content" style={{ maxWidth: 720 }}>
      <h1 className="page-heading" style={{ marginBottom: 20 }}>🏠 이웃 피드</h1>

      {error && <div className="alert alert-error">{error}</div>}

      {posts.length === 0 && !loading && !error ? (
        <div className="empty-state">
          <p style={{ fontSize: 32, marginBottom: 12 }}>📭</p>
          <p>팔로우한 블로거의 새 글이 여기에 표시됩니다.</p>
          <button className="btn btn-ghost" style={{ marginTop: 12 }} onClick={() => navigate('/posts')}>
            블로그 탐색하기
          </button>
        </div>
      ) : (
        <ul className="post-list">
          {posts.map((post) => (
            <li
              key={post.id}
              className="post-item"
              onClick={() => navigate(`/posts/${post.id}`)}
              style={{ cursor: 'pointer' }}
            >
              <div style={{ fontSize: 12, color: 'var(--text-light)', marginBottom: 4 }}>
                <span
                  onClick={(e) => { e.stopPropagation(); navigate(`/blog/${post.author_username}`); }}
                  style={{ cursor: 'pointer', color: 'var(--accent)' }}
                >
                  {post.author_username}
                </span>
              </div>
              <div className="post-title">{post.title}</div>
              {post.excerpt && <div className="post-excerpt">{post.excerpt}</div>}
              {post.tags && post.tags.length > 0 && (
                <div style={{ marginTop: 6 }}>
                  <TagCloud tags={post.tags} />
                </div>
              )}
              <div className="post-meta">
                <span>{new Date(post.created_at).toLocaleDateString('ko-KR', { year: 'numeric', month: 'long', day: 'numeric' })}</span>
                <span>·</span>
                <span>👁 {post.view_count ?? 0}</span>
                <span>·</span>
                <span>♥ {post.like_count ?? 0}</span>
              </div>
            </li>
          ))}
        </ul>
      )}

      <div ref={sentinelRef} style={{ height: 1 }} />
      {loading && <div className="empty-state" style={{ marginTop: 24 }}>불러오는 중...</div>}
      {!hasMore && posts.length > 0 && (
        <div style={{ textAlign: 'center', color: 'var(--text-light)', fontSize: 13, padding: '24px 0' }}>
          더 이상 새 글이 없습니다.
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: App.jsx에 /feed 라우트 추가**

import 추가:
```jsx
import Feed from './pages/Feed';
```

Routes에 `/search` 아래 추가:
```jsx
<Route path="/feed" element={<Feed />} />
```

- [ ] **Step 3: Nav.jsx에 "피드" 링크 추가**

editor 링크 목록에서 "내 블로그" 링크 앞에 추가:
```jsx
<Link to="/feed" className="nav-link">피드</Link>
```

- [ ] **Step 4: ESLint 확인**

```bash
docker compose exec -T frontend npx eslint \
  src/pages/Feed.jsx src/App.jsx src/components/Nav.jsx 2>&1
```

- [ ] **Step 5: 동작 확인**

1. `/blog/:username` → 팔로우 버튼 표시, 클릭 시 "팔로잉 ✓" 변경
2. 자신의 블로그 → 팔로우 버튼 없음
3. `/feed` → 팔로우한 블로거의 최신 포스트 표시
4. Nav "피드" 링크 클릭 → /feed 이동

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/pages/Feed.jsx frontend/src/App.jsx frontend/src/components/Nav.jsx
git commit -m "feat: /feed 이웃 피드 페이지 + Nav 피드 링크"
```

---

## 완료 체크리스트

- [ ] `docker compose exec backend pytest tests/ -v` — 전체 통과
- [ ] `POST /api/users/:username/follow` → 팔로우 201, 자신 팔로우 400
- [ ] `/blog/:username` → 팔로우 버튼 + 팔로워/팔로잉 수 표시
- [ ] `/feed` → 팔로우한 블로거 포스트만 표시
- [ ] Nav "피드" 링크 → /feed 이동
- [ ] GitHub Projects `[Phase 3] 구독/이웃` → Done 변경
